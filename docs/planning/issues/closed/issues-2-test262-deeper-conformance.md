---
id: issues-2
created_at: "2026-07-26"
updated_at: "2026-07-26"
area: planning
domain: language
title: "Test262 / deeper conformance"
description: "Wire official Test262 (or a curated subset) and promote failures into a new Roadmap backlog."
status: closed
issue-type: feature-request
severity: high
tags:
  - planning
  - issue
  - enhancement
  - closed
  - conformance
  - phase-2
---

# Test262 / deeper conformance

## Description

Wire official Test262 (or a curated subset) as the real “full ES” bar. Use failures to spawn a new E19+ (or parallel) backlog of atomic Roadmap items.

Internal fixtures (~199) prove Loop progress, not 262 completeness.

## Affected

- `tests/conformance` harness
- JS and native runners
- `ROADMAP.md` (new rows from failures)

## Observed

No Test262 integration; completeness is internal-fixture only.

## Impact

Cannot claim full ECMAScript fidelity; regressions vs the external standard are invisible.

## Proposed Fix

1. Record decision (ADR or note): full Test262 vs curated subset vs staged roll-in.
2. Harness runs selected tests on `js` and/or `native`.
3. Baseline pass/fail/skip report (checked in or CI-local script).
4. Process to promote failures into Roadmap child rows or vault issues.
5. At least one non-trivial failing area → concrete `todo` items.

## Human Brief

### Goal

Decide how (and whether) official Test262 becomes the external conformance bar, then hand off harness + backlog work as agent units.

### Why human

Product/scope judgment: full suite vs curated subset vs staged roll-in, and whether native is in scope for v1. An agent must not invent that ADR.

### Decisions needed

1. **Scope:** full Test262 | curated subset | staged roll-in (which stage first).
2. **Targets:** `js` only first, or `js` + `native` from day one.
3. **Vendor strategy:** submodule / sparse checkout / pinned tarball / CI download.
4. **Failure process:** auto-spawn Roadmap rows vs vault issues vs report-only until triage.

### After decisions

Split into agent-ready units, e.g.:

- ADR documenting the four decisions above.
- Harness runs selected tests on chosen targets.
- Baseline pass/fail/skip report.
- Promote at least one non-trivial fail area into concrete Roadmap `todo`s.

### Context (verified 2026-07-26)

- No Test262 wiring in-repo (mentions only on planning notes).
- ~199 internal fixtures; honesty pass left ~21 Roadmap `todo`s (N08*, T07, E17.02, E18.44) — internal bar only.
- Related: [[issues-1-roadmap-honesty-pass]] (closed).

### Out of scope for this issue

- Implementing missing language features beyond filing rows.
- Claiming full ES fidelity before a chosen bar is green.

## Comments

Related: [[issues-1-roadmap-honesty-pass]]

> *This was generated by AI during triage.*
>
> **2026-07-26:** Category `enhancement`. Verified not implemented (no harness/integration). No out-of-scope match. Scope ADR is judgment → `ready-for-human`. Residual Loop work can stay on existing Roadmap `todo`s until this decision lands.

> *This was generated by AI during implement.*
>
> **2026-07-26 — closed (implement).** Human-gated decisions locked and landed:
>
> 1. **Scope:** staged roll-in — harness + curated allowlist first (not full Test262 day one).
> 2. **Targets:** `js` only for v1.
> 3. **Vendor:** pinned commit via `scripts/fetch-test262.mjs` → gitignored `third_party/test262/` (do not commit the suite).
> 4. **Failures:** baseline JSON/Markdown report only; promote to Roadmap after triage. Seeded **E19.02** todo for allowlist expansion / first failure cluster.
>
> **Landed:**
> - `docs/adr/0007-test262-staged-roll-in.md`
> - `scripts/fetch-test262.mjs` (pinned SHA `07dbcbca04c5ac73eefd752eb0a67a893c159374`)
> - `tests/test262/` crate (`draconic-test262`): allowlist, compile→emit_js→node path, skip when suite missing, `baseline-report.md` on run
> - Workspace `Cargo.toml` + `.gitignore` (`third_party/test262/`, generated report)
> - `ROADMAP.md` **E19**: E19.01 `done` (harness), E19.02 `todo` (expand / promote)
>
> **How to fetch + run:**
> ```sh
> node scripts/fetch-test262.mjs
> cargo test -p draconic-test262
> # report: tests/test262/baseline-report.md and target/test262-baseline-report.md
> ```
