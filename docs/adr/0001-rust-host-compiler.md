# Rust host compiler

The Compiler is written in Rust permanently. Self-hosting in Draconic is out of scope. Rust gives a durable LLVM story, strong testing, and one language across Frontend, IR, and backends without forcing a multi-year bootstrap.
