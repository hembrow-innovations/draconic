# TypeScript-inspired types, not tsc compatibility

The Checker is inspired by TypeScript’s look and ergonomics but does not aim to compile existing TypeScript projects or match tsc flag-for-flag. The JS backend emits JavaScript, not TypeScript. Drop-in TS migration is out of scope so Native types and dual worlds are not constrained by tsc’s type erasure model.
