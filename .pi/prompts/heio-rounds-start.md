---
description: Start a file-backed planning or wayfinder sitting
argument-hint: "planning|wayfinder [slug]"
---

Load **heio-rounds**. The argument is `planning` or `wayfinder`, then an optional slug.

$ARGUMENTS
