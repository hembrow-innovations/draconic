---
id: "s-h15"
title: "H15 Subprocess"
kind: slice
status: active
sprint: "platform"
tags: []
created_at: "2026-09-02T12:09:20Z"
updated_at: "2026-09-02T21:11:08Z"
---

# H15 Subprocess

## Why

ROADMAP H15 locks the Host subprocess surface on both targets (ADR-0008 host I/O). H15.01–H15.03 already land spawn/run (argv, env subset, cwd, wait exit code), capture stdout/stderr plus stdin write and kill, and native async wait via the job queue; this cut is the parent row that those ops form one honest subprocess surface.

## Done

ROADMAP H15 is implemented test-first on both targets. A Program can spawn/run a child with argv, env subset, and cwd and wait for an exit code; capture stdout/stderr, write stdin, and kill the child; and on native await child exit through the job queue / Promise. Tests under `tests/conformance/host/process` (subprocess fixtures) lock that combined surface. Mark H15 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **H15.01**: spawn/run argv, env subset, cwd, wait exit code (already `done`)
- **H15.02**: capture stdout/stderr; write stdin; kill child (already `done`)
- **H15.03**: async subprocess exit via job queue / Promise (already `done`)
- **H01**: process args, env, exit
- **H14**: signals
- **H16**: OS misc
- **H00**: host I/O surface policy

## Oracle checklist

- [ ] O1: H15 subprocess spawn/run, capture/kill, and native async wait are locked by the host process conformance tests
  CHECK: cargo test -p draconic-conformance --test host_process
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: workspace tests stay green after the H15 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

- [[h15-subprocess]]

## See also

ROADMAP.md H15, `tests/conformance/tests/host_process.rs`, `tests/conformance/fixtures/host/process`, `crates/draconic-backend-llvm/src/host_subprocess.rs`, `crates/draconic-runtime`, docs/adr/0008-host-io-sockets-first-http.md, CONTEXT.md, [[host-io]], [[ticket-46-h15-subprocess]].
