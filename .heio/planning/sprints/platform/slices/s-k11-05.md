---
id: "s-k11-05"
title: "K11.05 Yank/retract when advisory source configured"
kind: slice
status: released
sprint: "platform"
tags: []
created_at: "2026-09-02T12:27:35Z"
updated_at: "2026-09-03T05:16:34Z"
---

# K11.05 Yank/retract when advisory source configured

## Why

ROADMAP K11.05 locks post-v1 yank/retract on the compiler target (ADR-0009 git-backed packages). When an advisory source is configured, a yanked or retracted version is refused at resolve/fetch instead of silently installing.

## Done

ROADMAP K11.05 is implemented test-first on the compiler target. With an advisory source configured, package resolve/fetch hard-fails a yanked or retracted version and does not pin it; with no advisory source configured, yank is not invented as a silent v1 check. Tests in `crates/draconic-pkg` lock that refuse path. Mark K11.05 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **K11**: Post-v1 packaging umbrella (not this child)
- **K11.01**: Private git auth (HTTPS token / SSH)
- **K11.02**: `replace` directive: fork/local override
- **K11.03**: Multi-module monorepo (subdir module paths)
- **K11.04**: Module proxy/mirror (git still canonical)
- **K08**: lock-hash integrity / tampered cache (already a v1 bar)
- npm unpublish or crates.io yank as the primary shape

## Oracle checklist

- [ ] O1: K11.05 yank/retract with an advisory source configured is locked by the draconic-pkg yank tests
  CHECK: cargo test -p draconic-pkg yank
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: workspace tests stay green after the K11.05 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

- [[k11-05-yank-retract-when-advisory-source]]

## See also

ROADMAP.md K11.05, `crates/draconic-pkg`, docs/adr/0009-go-style-git-packages.md, CONTEXT.md, [[locations/packages]], [[ticket-63-k11-05-yank-retract-when-advisory-source]].
