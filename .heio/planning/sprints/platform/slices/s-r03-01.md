---
id: "s-r03-01"
title: "R03.01 Integration: tampered cache refused (depends K08)"
kind: slice
status: active
sprint: "platform"
tags: []
created_at: "2026-09-02T11:28:12Z"
updated_at: "2026-09-02T21:11:08Z"
---

# R03.01 Integration: tampered cache refused (depends K08)

## Why

ROADMAP R03.01 makes supply-chain tamper refusal honest at integration: once **K08** lock-verify-refuse-tamper exists, `tests/integration` locks that a tampered module cache cannot silently succeed. K08.01 and K08.02 already land tree SHA-256 and OID/hash refuse in `draconic-pkg`; this cut is the child row that those checks are proven through the compiler integration surface when cache contents have been altered after the lock pin.

## Done

ROADMAP R03.01 is implemented test-first on the compiler target. Integration tests under `tests/integration` lock that a tampered cache is refused (no silent wrong tree). Mark R03.01 `done` only when those tests are green.

## Blocked by

None. K08.01 and K08.02 already land the pkg integrity checks this row tests.

## Non-goals

- **R03 parent remainder**: the umbrella lock-verify-refuse-tamper row
- **R03.02**: Integration: lock hash mismatch hard-fails build
- **K08**: Integrity: verify lock hashes; refuse tampered cache (pkg surface)
- **K08.01 / K08.02**: already `done` in `crates/draconic-pkg`
- **K09**: E2E temp git dep + consumer Program
- **R02**: permission model (fs/net grants)

## Oracle checklist

- [ ] O1: R03.01 tampered cache refused is locked by the integration suite
  CHECK: cargo test -p draconic-integration-tests --test supply_chain_tampered_cache
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: workspace tests stay green after the R03.01 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

- [[r03-01-integration-tampered-cache-refused]]

## See also

ROADMAP.md R03.01, `tests/integration`, `crates/draconic-pkg`, docs/adr/0009-go-style-git-packages.md, CONTEXT.md, [[runtime-hardening]], [[s-r03]], [[ticket-111-r03-01-integration-tampered-cache-refused-depends]].
