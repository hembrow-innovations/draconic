---
id: "s-l07"
title: "L07 Flags/CLI parse: argv → typed options/positionals"
kind: slice
status: ready
sprint: "platform"
tags: []
created_at: "2026-09-02T12:51:30Z"
updated_at: "2026-09-02T21:10:18Z"
---

# L07 Flags/CLI parse: argv → typed options/positionals

## Why

ROADMAP L07 locks the stdlib flags/CLI parse surface on both targets (stdlib location: honest portable libs a simple service needs). Long/short flags plus positionals and typed options (bool/string/number) with help text as designed are the ops; this cut is the parent row that those ops form one argv → typed options/positionals library a Program can use.

## Done

ROADMAP L07 is implemented test-first on both targets. A Program can parse argv into typed options and leftover positionals through the designed flags surface. Tests under `tests/conformance` fixtures `stdlib/flags` lock that combined surface. Mark L07 `done` only when those tests are green.

## Blocked by

None. The ticket names no dependency. L07.01 and L07.02 land the per-class long/short/positional parse and typed-options/help fixtures this parent row unifies.

## Non-goals

- **L07.01**: Parse long/short flags + positionals from string array
- **L07.02**: Typed options (bool/string/number); help text as designed
- **H01**: Process args/env/exit (host argv, not stdlib parse)
- **L05**: In-language test framework
- **L06**: Logging
- Node `util.parseArgs` identity or a full GNU getopt clone

## Oracle checklist

- [ ] O1: L07 long/short flags, positionals, and typed-options fixtures are locked by the stdlib flags conformance tests
  CHECK: cargo test -p draconic-conformance --test stdlib_flags
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: workspace tests stay green after the L07 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

## See also

ROADMAP.md L07, `tests/conformance/tests/stdlib_flags.rs`, `tests/conformance/fixtures/stdlib/flags`, CONTEXT.md, [[stdlib]], [[ticket-85-l07-flags-cli-parse-argv-typed]].
