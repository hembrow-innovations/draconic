---
id: "s-l05"
title: "L05 In-language test framework (`describe`/`it`/`expect` or designed) via `draconic test`"
kind: slice
status: ready
sprint: "platform"
tags: []
created_at: "2026-09-02T12:48:00Z"
updated_at: "2026-09-03T05:16:34Z"
---

# L05 In-language test framework (`describe`/`it`/`expect` or designed) via `draconic test`

## Why

ROADMAP L05 locks the in-language test framework on both targets (stdlib location: honest portable libs a simple service needs). L05.01–L05.04 already land `describe`/`it` registration via `draconic test`, `expect` matchers with failure messages, nested describe plus before/after hooks, and CLI aggregate exit with the fixture runner; this cut is the parent row that those ops form one testing library a Program can run with `draconic test`.

## Done

ROADMAP L05 is implemented test-first on both targets. A Program can register tests with `describe`/`it` (or designed names), assert with `expect` matchers, nest describes with hooks as designed, and `draconic test` runs that suite and aggregates its exit with fixture results. Tests under `tests/conformance` fixtures `stdlib/testing` and `crates/draconic-cli` lock that combined surface. Mark L05 `done` only when those tests are green.

## Blocked by

None. L05.01, L05.02, L05.03, and L05.04 already land the per-class register/run, matcher, nested-hooks, and CLI-aggregate fixtures this parent row unifies.

## Non-goals

- **L05.01**: `describe` / `it` (or designed) register tests; run via `draconic test` (already `done`)
- **L05.02**: `expect` matchers: equality, truthiness; failure messages (already `done`)
- **L05.03**: Nested describe; before/after hooks as designed (already `done`)
- **L05.04**: CLI aggregates in-language suite exit codes with fixture runner (already `done`)
- **C04**: Parallel `draconic test` worker pool
- **L06**: Logging
- Test262 runner (S02 / E19.02)

## Oracle checklist

- [ ] O1: L05 describe/it/expect and nested-hooks fixtures are locked by the stdlib testing conformance tests
  CHECK: cargo test -p draconic-conformance --test stdlib_testing
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: L05 `draconic test` run and CLI aggregate-exit behavior stay locked by the CLI test command tests
  CHECK: cargo test -p draconic-cli --test test_cmd
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O3: workspace tests stay green after the L05 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

## See also

ROADMAP.md L05, `tests/conformance/tests/stdlib_testing.rs`, `tests/conformance/fixtures/stdlib/testing`, `crates/draconic-cli/tests/test_cmd.rs`, `crates/draconic-backend-llvm/src/es_testing.rs`, CONTEXT.md, [[stdlib]], [[ticket-83-l05-in-language-test-framework-describe]].
