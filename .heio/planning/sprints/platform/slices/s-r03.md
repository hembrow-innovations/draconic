---
id: "s-r03"
title: "R03 Supply-chain policy tests once K08 lands (lock verify refuse tamper)"
kind: slice
status: active
sprint: "platform"
tags: []
created_at: "2026-09-02T11:25:27Z"
updated_at: "2026-09-03T05:16:34Z"
---

# R03 Supply-chain policy tests once K08 lands (lock verify refuse tamper)

## Why

ROADMAP R03 makes supply-chain policy honest at integration: once **K08** lock-verify-refuse-tamper exists, `tests/integration` locks that a tampered cache or lock hash mismatch cannot silently succeed. K08.01 and K08.02 already land tree SHA-256 and OID/hash refuse in `draconic-pkg`; this cut is the parent row that those checks are proven through the compiler integration surface.

## Done

ROADMAP R03 is implemented test-first on the compiler target. Integration tests under `tests/integration` lock that lock-hash verify refuses tamper (tampered cache refused; lock hash mismatch hard-fails the build). Mark R03 `done` only when those tests are green.

## Blocked by

None. K08.01 and K08.02 already land the pkg integrity checks this row tests.

## Non-goals

- **R03.01**: Integration: tampered cache refused (depends K08)
- **R03.02**: Integration: lock hash mismatch hard-fails build
- **K08**: Integrity: verify lock hashes; refuse tampered cache (pkg surface)
- **K08.01 / K08.02**: already `done` in `crates/draconic-pkg`
- **K09**: E2E temp git dep + consumer Program
- **R02**: permission model (fs/net grants)

## Oracle checklist

- [ ] O1: R03 supply-chain policy (lock verify refuse tamper) is locked by the integration suite
  CHECK: cargo test -p draconic-integration-tests --test supply_chain
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: workspace tests stay green after the R03 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

- [[r03-supply-chain-policy-tests-once]]

## See also

ROADMAP.md R03, `tests/integration`, `crates/draconic-pkg`, docs/adr/0009-go-style-git-packages.md, CONTEXT.md, [[runtime-hardening]], [[ticket-110-r03-supply-chain-policy-tests-once]].
