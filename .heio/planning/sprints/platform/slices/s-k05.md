---
id: "s-k05"
title: "K05 CLI: `draconic get`, `draconic mod tidy`"
kind: slice
status: active
sprint: "platform"
tags: []
created_at: "2026-09-02T12:17:18Z"
updated_at: "2026-09-02T21:11:08Z"
---

# K05 CLI: `draconic get`, `draconic mod tidy`

## Why

ROADMAP K05 locks the package CLI surface on the compiler target (ADR-0009 git-backed packages). K05.01–K05.02 already land `draconic get <module_path>@<ver>` (fetch, update manifest+lock+cache) and `draconic mod tidy` (lock matches manifest; fetch missing; prune unused); this cut is the parent row that those ops form one honest get/tidy CLI.

## Done

ROADMAP K05 is implemented test-first on the compiler target. `draconic get` fetches a module path@version and writes manifest, lock, and cache. `draconic mod tidy` makes the lock match the manifest, fetches missing deps, and prunes unused pins. Tests in `crates/draconic-cli` (`get`, `mod_tidy`) lock that combined surface. Mark K05 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **K05.01**: `draconic get <module_path>@<ver>`: fetch, update manifest+lock+cache (already `done`)
- **K05.02**: `draconic mod tidy`: lock matches manifest; fetch missing; prune unused (already `done`)
- **K01**: Manifest (`draconic.toml`)
- **K02**: Lockfile (`draconic.lock`)
- **K03**: Module cache layout / git clone
- **K04**: Version resolve (semver tag → commit OID)
- **K07**: Build integration auto-fetch / offline
- **K08**: Integrity verify lock hashes; refuse tampered cache

## Oracle checklist

- [ ] O1: K05 `draconic get` is locked by the draconic-cli get tests
  CHECK: cargo test -p draconic-cli --test get
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: K05 `draconic mod tidy` is locked by the draconic-cli mod_tidy tests
  CHECK: cargo test -p draconic-cli --test mod_tidy
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O3: workspace tests stay green after the K05 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

- [[k05-cli-draconic-get-draconic-mod]]

## See also

ROADMAP.md K05, `crates/draconic-cli/tests/get.rs`, `crates/draconic-cli/tests/mod_tidy.rs`, `crates/draconic-pkg/src/get.rs`, `crates/draconic-pkg/src/tidy.rs`, docs/adr/0009-go-style-git-packages.md, CONTEXT.md, [[packages]], [[ticket-54-k05-cli-draconic-get-draconic-mod]].
