---
id: "s-f02"
title: "F02 C callbacks: Draconic fn as extern C pointer; host invokes"
kind: slice
status: released
sprint: "platform"
tags: []
created_at: "2026-09-02T12:30:12Z"
updated_at: "2026-09-02T21:10:18Z"
---

# F02 C callbacks: Draconic fn as extern C pointer; host invokes

## Why

ROADMAP F02 locks C callbacks on native (Dual-world explicit boundary; ADR-0003 GC runtime and dual worlds). F02.01–F02.02 already land export of a Draconic fn as a C function pointer and host invoke with scalar args so the return is observed; this cut is the parent row that those ops form one honest native callback surface.

## Done

ROADMAP F02 is implemented test-first on native. A Program can pass a Draconic fn as an `extern "C"` pointer and the host can invoke it with scalar args so the return value is observed. Tests under `tests/conformance` fixtures `ffi/callback` lock that combined surface. Mark F02 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **F02.01**: Export Draconic fn as C function pointer (already `done`)
- **F02.02**: Host invokes callback with scalar args; return value observed (already `done`)
- **F01**: `extern "C"` call out (scalar args/returns)
- **F03**: C-compatible struct layout
- **F04**: Link external static lib
- **F05**: Load dynamic lib
- **F08**: Unsafe/native-only FFI diagnostics
- js FFI or a silent JS callback polyfill (native-only; other backend hard-errors)

## Oracle checklist

- [ ] O1: F02 export-as-fnptr and host scalar invoke are locked by the ffi callback conformance tests
  CHECK: cargo test -p draconic-conformance --test ffi_callback
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: workspace tests stay green after the F02 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

- [[f02-c-callbacks]]

## See also

ROADMAP.md F02, `tests/conformance/tests/ffi_callback.rs`, `tests/conformance/fixtures/ffi/callback`, `crates/draconic-backend-llvm`, docs/adr/0003-gc-runtime-and-dual-worlds.md, CONTEXT.md, [[ffi]], [[ticket-64-f02-c-callbacks-draconic-fn-as]].
