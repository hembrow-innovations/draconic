---
id: "s-h11"
title: "H11 TLS"
kind: slice
status: released
sprint: "platform"
tags: []
created_at: "2026-09-02T12:06:00Z"
updated_at: "2026-09-03T05:16:34Z"
---

# H11 TLS

## Why

ROADMAP H11 locks the Host TLS surface on native (ADR-0008 sockets-first; TLS after plaintext sockets). H11.01–H11.03 already land client wrap (trust roots / insecure-test), server wrap (PEM cert/key + handshake), and HTTPS HTTP/1.1 over TLS loopback; this cut is the parent row that those ops form one honest TLS surface.

## Done

ROADMAP H11 is implemented test-first on native. A Program can wrap a TCP connection as a TLS client (`tlsClientWrap(conn, serverName, insecure)` with system trust roots when insecure=0 and skip-verify when insecure=1), wrap an accepted connection as a TLS server (`tlsServerWrap(conn, certPath, keyPath)` PEM), read/write/close via `tlsRead` / `tlsWrite` / `closeTls`, and complete HTTPS HTTP/1.1 over TLS on loopback. Tests under `tests/conformance/host/net/tls` lock that combined surface. Mark H11 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **H11.01**: TLS client wrap trust roots / insecure-test (already `done`)
- **H11.02**: TLS server wrap cert/key handshake (already `done`)
- **H11.03**: HTTPS HTTP/1.1 over TLS loopback (already `done`)
- **H06**: TCP listen/accept/connect/read/write
- **H10**: HTTP/1.1 thin helpers (plaintext)
- **H12**: WebSocket
- **H13**: HTTP/2
- **H00**: host I/O surface policy

## Oracle checklist

- [ ] O1: H11 TLS client/server wrap and HTTPS loopback are locked by the host tls conformance tests
  CHECK: cargo test -p draconic-conformance --test host_tls
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: workspace tests stay green after the H11 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

- [[h11-tls]]

## See also

ROADMAP.md H11, `tests/conformance/tests/host_tls.rs`, `tests/conformance/fixtures/host/net/tls`, `crates/draconic-backend-llvm/src/host_tcp.rs`, `crates/draconic-backend-llvm/src/host_http_server.rs`, `crates/draconic-runtime`, docs/adr/0008-host-io-sockets-first-http.md, CONTEXT.md, [[host-io]], [[ticket-42-h11-tls]].
