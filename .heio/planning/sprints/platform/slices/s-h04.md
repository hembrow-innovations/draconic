---
id: "s-h04"
title: "H04 Filesystem: read / write / dirs"
kind: slice
status: released
sprint: "platform"
tags: []
created_at: "2026-09-02T11:59:10Z"
updated_at: "2026-09-03T05:16:34Z"
---

# H04 Filesystem: read / write / dirs

## Why

ROADMAP H04 locks the Host filesystem surface on both targets: read, write, and directory ops. H04.01–H04.06 already land whole-file read/write, exists/stat, mkdir/readdir/rmdir, rename/copy/delete, and native open handles; this cut is the parent row that those ops form one honest filesystem surface.

## Done

ROADMAP H04 is implemented test-first on both targets. A Program can read whole-file bytes and UTF-8 text (missing → typed error), write/append/create/truncate, `exists` / `stat` (size, isFile, isDir, mtime), mkdir (optional recursive) / readdir / rmdir / remove file, and rename / copy / delete file. Native also opens an fd-like handle (open/read/write/seek/close). Tests under `tests/conformance/host/fs` lock that combined surface. Mark H04 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **H04.01**: File read: whole-file bytes + UTF-8 text; missing → typed error (already `done`)
- **H04.02**: File write / append; create/truncate (already `done`)
- **H04.03**: `exists` / `stat` (already `done`)
- **H04.04**: Directory mkdir / readdir / rmdir / remove file (already `done`)
- **H04.05**: Rename / copy / delete file (already `done`)
- **H04.06**: Open handle open/read/write/seek/close (already `done`)
- **H03**: path helpers (string ops; no I/O)
- **H00**: host I/O surface policy
- **R02**: permission grant/deny for fs

## Oracle checklist

- [ ] O1: H04 filesystem read / write / dirs are locked by the host fs conformance tests
  CHECK: cargo test -p draconic-conformance --test host_fs
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: workspace tests stay green after the H04 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

- [[h04-filesystem-read-write-dirs]]

## See also

ROADMAP.md H04, `tests/conformance/tests/host_fs.rs`, `tests/conformance/fixtures/host/fs`, `crates/draconic-backend-llvm/src/host_fs.rs`, docs/adr/0008-host-io-sockets-first-http.md, CONTEXT.md, [[host-io]], [[ticket-35-h04-filesystem-read-write-dirs]].
