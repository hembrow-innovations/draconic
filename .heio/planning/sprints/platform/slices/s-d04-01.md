---
id: "s-d04-01"
title: "D04.01 Cross-compile: at least one non-host triple smoke"
kind: slice
status: released
sprint: "platform"
tags: []
created_at: "2026-09-02T22:06:49Z"
updated_at: "2026-09-03T05:16:34Z"
---

# D04.01 Cross-compile: at least one non-host triple smoke

## Why

ROADMAP D04.01 is the non-host triple LLVM smoke on the distribution location. Parent D04 names the available linux/darwin/windows × amd64/arm64 compile surface and does not require a non-host success. This cut proves the LLVM backend can emit for at least one triple that is not the host.

## Done

ROADMAP D04.01 is implemented test-first on the native target. Compiling a Program through the LLVM backend succeeds for at least one non-host triple from the D04 matrix. Tests under `tests/integration` and `crates/draconic-backend-llvm` lock that smoke. Mark D04.01 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **D04 parent remainder**: available-OS/arch matrix surface without a non-host success
- **D04.02**: Matrix docs + CI jobs for available OS/arch pairs
- **D03**: Reproducible-build byte identity
- **D05**: Strip / LTO size flags
- **D01**: Release binaries + install script

## Oracle checklist

- [ ] O1: D04.01 non-host triple LLVM smoke is locked by the integration suite
  CHECK: cargo test -p draconic-integration-tests --test cross_compile_non_host
  EXPECT: test result: ok.
  EVIDENCE: cargo test -p draconic-integration-tests --test cross_compile_non_host → test result: ok. 1 passed

- [ ] O2: workspace tests stay green after the D04.01 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: cargo test --workspace --offline → all crates test result: ok.

## Pool

Durable links to task-pool ids. Never drop them.

- [[d04-01-cross-compile-at-least-one-non-host]]

## See also

ROADMAP.md D04.01, `tests/integration`, `crates/draconic-backend-llvm`, CONTEXT.md, [[distribution]], [[ticket-99-d04-01-cross-compile-at-least-one]].
