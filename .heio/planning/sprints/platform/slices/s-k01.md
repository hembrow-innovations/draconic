---
id: "s-k01"
title: "K01 Manifest (`draconic.toml`): module path, deps, optional path→git URL map"
kind: slice
status: active
sprint: "platform"
tags: []
created_at: "2026-09-02T12:13:41Z"
updated_at: "2026-09-02T21:11:08Z"
---

# K01 Manifest (`draconic.toml`): module path, deps, optional path→git URL map

## Why

ROADMAP K01 locks the git-backed package manifest surface (ADR-0009): `draconic.toml` carries the Program's own module path, a deps map (path → version req), and an optional path→git URL map when default URL derivation is wrong. K01.01–K01.04 already land parse, stable write/round-trip, schema diagnostics, and default `https://{module_path}.git`; this cut is the parent row that those ops form one honest manifest.

## Done

ROADMAP K01 is implemented test-first on the compiler. A Program's `draconic.toml` parses own module path plus deps, round-trips with stable order, rejects invalid schema with diagnostics, and resolves git URLs from the optional map or default derivation. Tests in `crates/draconic-pkg` lock that combined surface. Mark K01 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **K01.01**: parse `draconic.toml`: own module path + deps map (already `done`)
- **K01.02**: write/round-trip `draconic.toml` (already `done`)
- **K01.03**: manifest schema validation + diagnostics (already `done`)
- **K01.04**: optional URL map; default derive `https://{module_path}.git` (already `done`)
- **K02**: lockfile (`draconic.lock`)
- **K03**: module cache
- **K05**: CLI `draconic get` / `draconic mod tidy`
- **D02**: toolchain version pin in `draconic.toml`

## Oracle checklist

- [ ] O1: K01 manifest parse/write/validate/url-map are locked by the draconic-pkg lib tests
  CHECK: cargo test -p draconic-pkg --lib
  EXPECT: test result: ok.
  EVIDENCE: pending

- [ ] O2: workspace tests stay green after the K01 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: pending

## Pool

Durable links to task-pool ids. Never drop them.

- [[k01-manifest-draconic-toml-module-path]]

## See also

ROADMAP.md K01, `crates/draconic-pkg/src/lib.rs`, docs/adr/0009-go-style-git-packages.md, CONTEXT.md, [[packages]], [[ticket-50-k01-manifest-draconic-toml-module-path]].
