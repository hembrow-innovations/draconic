---
id: "ticket-915-js-library-export-star"
title: "JS library emit has no export star"
kind: ticket
status: promoted
ticket_type: feature-request
tags: [js, linker, distribution]
blocked_by: []
sprint: "js-library-esm"
created_at: "2026-09-12T11:34:32Z"
updated_at: "2026-09-12T18:20:00Z"
---

# JS library emit has no export star

## Signal

[[ticket-908-js-library-esm-export]] asked for library emit Node can import. [[slice-911-js-library-named-esm]] locked named exports and named re-exports only. A Module that authors `export *` still does not expose those names on the JS artifact.

## Fit

Promoted to [[slice-918-js-library-export-star]] / [[task-919-js-library-export-star]] in [[js-library-esm]], blocked by [[slice-916-js-library-default-export]]. Executable AFK: [[task-919-js-library-export-star]]. Not [[slice-911-js-library-named-esm]]. Not AFK from this ticket.

## Notes

- Named `--library` emit is in. `export *` was an explicit later child of [[ticket-908-js-library-esm-export]].
- A two-file `export { view } from` is already in slice 911. This signal is star re-export, not that named form.

## Parent

[[ticket-908-js-library-esm-export]]

## What to build

A Draconic library entry that authors `export *` can be built to JavaScript that Node can import the re-exported names from.

## Blocked by

None.

## Comments

> *This was generated by AI during filing.*

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Confirmed on this checkout. `--library` of `export * from "./dep.drac"` writes `const __m0_view = "view"` with no `export`. Node `import { view }` fails.
- Linker `entry_named_exports` does not include `export *` star names. Named `export { view } from` already works.
- Not already implemented. No `docs/` rejection. Distinct from default export.

**Verification result:** confirmed

**Recommended ticket_type:** feature-request

**Recommended status:** promoted

**One-line reason:** `export *` names never reach the JS library artifact.

**Applied:** Promoted to [[slice-918-js-library-export-star]] / [[task-919-js-library-export-star]], blocked by default-export.
