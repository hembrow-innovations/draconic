---
id: "ticket-915-js-library-export-star"
title: "JS library emit has no export star"
kind: ticket
status: open
ticket_type: feature-request
tags: [js, linker, distribution]
blocked_by: []
created_at: "2026-09-12T11:34:32Z"
updated_at: "2026-09-12T11:34:32Z"
---

# JS library emit has no export star

## Signal

[[ticket-908-js-library-esm-export]] asked for library emit Node can import. [[slice-911-js-library-named-esm]] locked named exports and named re-exports only. A Module that authors `export *` still does not expose those names on the JS artifact.

## Fit

This project, later slice. Not [[slice-911-js-library-named-esm]]. [[js-library-esm]] listed `export *` as out.

## Notes

- Named `--library` emit is in. `export *` was an explicit later child of [[ticket-908-js-library-esm-export]].
- A two-file `export { view } from` is already in slice 911. This signal is star re-export, not that named form.

## Parent

[[ticket-908-js-library-esm-export]]

## What to build

A Draconic library entry that authors `export *` can be built to JavaScript that Node can import the re-exported names from.

## Blocked by

None.

## Comments

> *This was generated by AI during filing.*
