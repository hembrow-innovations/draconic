---
id: "ticket-914-js-library-default-export"
title: "JS library emit has no default export"
kind: ticket
status: promoted
ticket_type: feature-request
tags: [js, linker, distribution]
blocked_by: []
sprint: "js-library-esm"
created_at: "2026-09-12T11:34:32Z"
updated_at: "2026-09-12T18:20:00Z"
---

# JS library emit has no default export

## Signal

[[ticket-908-js-library-esm-export]] asked for library emit Node can import. [[slice-911-js-library-named-esm]] locked named exports only. A Module that authors `export default` still has no default on the JS artifact.

## Fit

Promoted to [[slice-916-js-library-default-export]] / [[task-917-js-library-default-export]] in [[js-library-esm]]. Executable AFK: [[task-917-js-library-default-export]]. Not [[slice-911-js-library-named-esm]]. Not AFK from this ticket.

## Notes

- Named `--library` emit is in. Default export was an explicit later child of [[ticket-908-js-library-esm-export]].
- IR entry metadata from [[task-910-entry-export-table]] is the named-export table.

## Parent

[[ticket-908-js-library-esm-export]]

## What to build

A Draconic library entry that authors a default export can be built to JavaScript that Node can import as the module default.

## Blocked by

None.

## Comments

> *This was generated by AI during filing.*

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Confirmed on this checkout. `--library` of `export default "def"; export const named = "n"` writes `let __default = "def";` and `export { named }`. Node `import d from` fails: no export named `default`.
- Linker `entry_named_exports` skips public name `default`. Flatten already binds `__default`. Named `--library` emit is in.
- Not already implemented. No `docs/` rejection. Sprint first cut listed this as out of slice-911, not out of the project.

**Verification result:** confirmed

**Recommended ticket_type:** feature-request

**Recommended status:** promoted

**One-line reason:** Authored `export default` still has no default on the JS library artifact.

**Applied:** Promoted to [[slice-916-js-library-default-export]] / [[task-917-js-library-default-export]].
