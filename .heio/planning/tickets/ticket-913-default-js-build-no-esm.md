---
id: "ticket-913-default-js-build-no-esm"
title: "Default JS build still has no ESM named export"
kind: ticket
status: parked
ticket_type: observation
tags: [js, linker, distribution]
blocked_by: []
sprint: "js-library-esm"
created_at: "2026-09-12T11:34:32Z"
updated_at: "2026-09-12T18:20:00Z"
---

# Default JS build still has no ESM named export

## Signal

GitHub issue 1 (`hembrow-innovations/draconic`) still matches default `draconic build --target js`. A Module that authors `export const view = "view"` emits `const view = "view"` with no `export`. Node `import { view } from` that artifact fails.

[[slice-911-js-library-named-esm]] added opt-in `--library`. The issue reproduction does not pass `--library`.

## Fit

This project, later if ever always-ESM. Not a defect against the locked contract. GitHub issue 1 remainder for default `build --target js` is answered as opt-in `--library` for now. [[js-library-esm]] listed always-ESM and changing `draconic run` as out.

## Notes

- Facts: default build remains a script. `draconic run --target js` still spawns `node` with no `--input-type=module`.
- Opt-in `--library` is not the command in the issue body.
- Do not treat this as a printer bug. Same peel-and-flatten path as [[ticket-908-js-library-esm-export]].

## Parent

[[ticket-908-js-library-esm-export]]

## What to build

The GitHub issue 1 reproduction (`draconic build --target js` on a named-export Module, no `--library`) yields an artifact Node can `import { name } from`, or the issue is answered as out of default build.

## Blocked by

None.

## Comments

> *This was generated by AI during filing.*

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Confirmed on this checkout. Default `draconic build --target js` of `export const view = "view"` writes `const view = "view"`. Node `import { view }` fails. That matches GitHub issue 1's command, which does not pass `--library`.
- Not a regression. `toolchain.cli:build-js-library-esm` locks default build and run as scripts. `build_js_without_library_stays_script` is the named test. Always-ESM would SyntaxError `draconic run` unless the host becomes a module.
- No `docs/` rejection of a later always-ESM sitting. Not already implemented as default emit.

**Verification result:** confirmed (locked intended behaviour for default build)

**Recommended ticket_type:** observation

**Recommended status:** parked

**One-line reason:** Issue 1 still matches default build; the locked answer is `--library`, not always-ESM.

**Applied:** Parked. Retyped from bug to observation. Did not write a durable rejection note.
