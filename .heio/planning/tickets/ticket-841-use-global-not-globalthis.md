---
id: "ticket-841-use-global-not-globalthis"
title: "Use global instead of globalThis for console bind"
kind: ticket
status: open
ticket_type: feature-request
tags: [needs-triage]
blocked_by: []
references: ["rounds-842-global-object-identifier"]
created_at: "2026-09-12T05:31:40Z"
updated_at: "2026-09-12T05:50:00Z"
---

# Use global instead of globalThis for console bind

## Signal

Conversation 2026-09-12. Request to change `let console = globalThis.console` to use `global` instead of `globalThis`.

## Fit

Escalated. Replacing `globalThis` everywhere would rewrite [[location-218-conformance]] and ADR-0004. Wayfinder: [[rounds-842-global-object-identifier]]. Do not execute from this ticket.

## Notes

- Documented bind is `let console = globalThis.console` because free identifier `console` is unresolved. Same line in README, `examples/shebang/hello.drac`, `examples/fizzbuzz/main.drac`, `docs/guides/guides-toolchain.md`.
- Check installs builtin `globalThis` only (`crates/draconic-check/src/binder.rs`).
- ROADMAP E15.01 locks `globalThis` as the global object.
- Native `globalThis.console.log` lowering already shipped ([[ticket-773-native-console-log]], closed).

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Feature request, not a bug. Checker installs `globalThis` only. Teaching and tests lock `let console = globalThis.console`.
- Maintainer asked to promote, then to replace `globalThis` everywhere. That is a location rewrite, not a platform slice.
- Wayfinder round [[rounds-842-global-object-identifier]] is charted. Research [[ticket-843-ecma-host-global-names]] closed: ECMA-262 name is `globalThis`; Node `global` is legacy.

**What we still need from you:**

- Resolve frontier HITL [[ticket-844-program-visible-globalthis]]: does a Program still see `globalThis`?
