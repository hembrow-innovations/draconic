---
id: "s-p05"
title: "P05 Shebang support docs + `#!/usr/bin/env draconic` run path (with **U14**)"
kind: slice
status: met
sprint: "platform"
tags: []
created_at: "2026-09-02T11:41:44Z"
updated_at: "2026-09-05T13:17:15Z"
---

# P05 Shebang support docs + `#!/usr/bin/env draconic` run path (with **U14**)

## Why

ROADMAP P05 makes shebang execution honest as a product path: docs name `#!/usr/bin/env draconic`, an in-repo Program under `examples/` uses that line, and the CLI run path (U14) executes it. U14 already lands `draconic run` and shebang-friendly invoke; this cut is the documented run path plus an example a stranger can chmod and execute.

## Done

ROADMAP P05 is implemented test-first on the compiler target. Docs (README and/or Learn/Reference CLI) name `#!/usr/bin/env draconic`. An example under `examples/` starts with that shebang and runs via the U14 path. Tests under `crates/draconic-cli` lock the documented shebang run path against that example. Mark P05 `done` only when those tests are green.

## Blocked by

None. U14 is already `done` (`draconic run` + shebang-friendly invoke).

## Non-goals

- **U14**: `draconic run <file>` convenience itself (already `done`)
- **P01**: fizzbuzz flagship Program
- **P03**: language book / docs site skeleton
- **P04**: flagship service example (typed HTTP + fs/config + git dep)
- **D01**: release binaries / install script
- Changing the U14 default target policy

## Oracle checklist

- [x] O1: P05 shebang docs + `#!/usr/bin/env draconic` run path (with U14) is locked, including an `examples/` Program
  CHECK: cargo test -p draconic-cli --test shebang
  EXPECT: test result: ok.
  EVIDENCE: met exit=0 match=yes sha256=ffc899ed4d6810cd bytes=2593 at=2026-09-05T13:11:20.474Z

- [x] O2: workspace tests stay green after the P05 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: met exit=0 match=yes sha256=96844e066a4eac15 bytes=189498 at=2026-09-05T13:16:50.293Z

## Pool

Durable links to task-pool ids. Never drop them.

- [[p05-shebang-support-docs-usr-bin]]

## See also

ROADMAP.md P05, ROADMAP.md U14, `crates/draconic-cli`, `crates/draconic-cli/tests/run.rs`, `examples/`, `website/cli.md`, README.md, CONTEXT.md, [[product]], [[ticket-118-p05-shebang-support-docs-usr-bin]].
