---
id: "s-k11-02"
title: "K11.02 `replace` directive: fork/local override"
kind: slice
status: failed
sprint: "platform"
tags: []
created_at: "2026-09-02T12:23:53Z"
updated_at: "2026-09-04T19:07:19Z"
claimed-by: 48432047-7f7f-4c72-a017-9aaf9be4a858
---

# K11.02 `replace` directive: fork/local override

## Why

ROADMAP K11.02 locks the post-v1 `replace` directive on the compiler target (ADR-0009 git-backed packages). A Program can override a module path to a fork (different git URL / module path) or a local directory so resolve and fetch use the replacement instead of the declared dependency identity.

## Done

ROADMAP K11.02 is implemented test-first on the compiler target. `draconic.toml` accepts a `replace` directive that maps a module path to a fork git source or a local path; package resolve/fetch honors that override and does not silently keep the original pin. Tests in `crates/draconic-pkg` lock parse and apply. Mark K11.02 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **K11**: Post-v1 packaging umbrella (not this child)
- **K11.01**: Private git auth (HTTPS token / SSH)
- **K11.03**: Multi-module monorepo (subdir module paths)
- **K11.04**: Module proxy/mirror (git still canonical)
- **K11.05**: Yank/retract when advisory source configured
- **K01**: Manifest parse/write/url-map (already landed)
- npm-style `resolutions` or crates.io `[patch]` as the primary shape

## Oracle checklist

- [x] O1: K11.02 `replace` fork and local override are locked by the draconic-pkg replace tests
  CHECK: cargo test -p draconic-pkg replace
  EXPECT: test result: ok.
  EVIDENCE: met exit=0 match=yes sha256=668e413cad3bfa55 bytes=1653 at=2026-09-04T19:04:42.748Z

- [ ] O2: workspace tests stay green after the K11.02 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: unmet exit=timeout match=yes bytes=88333 at=2026-09-04T19:06:42.751Z
  ABANDON: leftover after --reverify; CHECK timed out at 120s (exit=timeout match=yes bytes=88333); home [[ticket-174-k11-02-workspace-timeout]]

## Pool

Durable links to task-pool ids. Never drop them.

- [[k11-02-replace-directive-fork-local-override]]

## See also

ROADMAP.md K11.02, `crates/draconic-pkg/src/lib.rs`, docs/adr/0009-go-style-git-packages.md, CONTEXT.md, [[locations/packages]], [[ticket-60-k11-02-replace-directive-fork-local-override]].
