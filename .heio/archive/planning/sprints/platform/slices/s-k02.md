---
id: "s-k02"
title: "K02 Lockfile (draconic.lock): resolved pins"
kind: slice
status: failed
sprint: "platform"
tags: []
created_at: "2026-09-02T12:14:16Z"
updated_at: "2026-09-04T18:17:04Z"
claimed-by: 0947cc3e-9610-4291-b0f7-4b47e19a2105
---

# K02 Lockfile (draconic.lock): resolved pins

## Why

ROADMAP K02 locks the package lockfile surface on the compiler target (ADR-0009 git-backed packages). K02.01–K02.03 already land lock entries (path + version + git URL + commit OID + content hash SHA-256), parse/write with reject-malformed, and stable serialize (sorted paths; byte-identical rewrite when unchanged); this cut is the parent row that those ops form one honest `draconic.lock` resolved-pins surface.

## Done

ROADMAP K02 is implemented test-first on the compiler target. A Program's package graph can pin resolved deps in `draconic.lock`: each entry carries path, version, git URL, commit OID, and tree SHA-256; parse/write reject malformed locks; rewrite of an unchanged lock is byte-identical with packages sorted by path. Tests in `crates/draconic-pkg` lock that combined surface. Mark K02 `done` only when those tests are green.

## Blocked by

None.

## Non-goals

- **K02.01**: Lock entry: path + version + git URL + commit OID + content hash SHA-256 (already `done`)
- **K02.02**: Parse/write lock; reject malformed (already `done`)
- **K02.03**: Stable lock serialize: sorted paths; byte-identical rewrite when unchanged (already `done`)
- **K01**: Manifest (`draconic.toml`)
- **K03**: Module cache layout / git clone
- **K04**: Version resolve (semver tag / commit)
- **K08**: Integrity verify lock hashes; refuse tampered cache

## Oracle checklist

- [x] O1: K02 lockfile resolved pins are locked by the draconic-pkg lock tests
  CHECK: cargo test -p draconic-pkg lock
  EXPECT: test result: ok.
  EVIDENCE: met exit=0 match=yes sha256=93b53c2a0e907f36 bytes=4154 at=2026-09-04T18:14:29.651Z

- [ ] O2: workspace tests stay green after the K02 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: unmet exit=timeout match=yes bytes=77246 at=2026-09-04T18:16:29.654Z
  ABANDON: leftover after --reverify; CHECK timed out at 120s (exit=timeout match=yes bytes=77246); home [[ticket-166-k02-workspace-timeout]]

## Pool

Durable links to task-pool ids. Never drop them.

- [[k02-lockfile-draconic-lock-resolved-pins]]

## See also

ROADMAP.md K02, `crates/draconic-pkg/src/lock.rs`, `crates/draconic-pkg`, docs/adr/0009-go-style-git-packages.md, CONTEXT.md, `.heio/planning/locations/packages.md`, [[ticket-51-k02-lockfile-draconic-lock-resolved-pins]].
