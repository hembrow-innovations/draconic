---
id: "s-l06"
title: "L06 Logging: leveled logger; stderr/stdout sink"
kind: slice
status: failed
sprint: "platform"
tags: []
created_at: "2026-09-02T12:50:43Z"
updated_at: "2026-09-04T21:00:28Z"
claimed-by: 4d856893-0d18-46ce-89f3-b06945239fbc
---

# L06 Logging: leveled logger; stderr/stdout sink

## Why

ROADMAP L06 locks the leveled logger and stderr/stdout sink on both targets (stdlib location: honest portable libs a simple service needs). L06.01 and L06.02 already land `createLogger` with error/warn/info/debug plus level filter, and the string-format stdio sink (debug/info → stdout, warn/error → stderr); this cut is the parent row that those ops form one logging library a Program can use.

## Done

ROADMAP L06 is implemented test-first on both targets. A Program can create a leveled logger, filter by level, and sink formatted lines to stderr/stdout as designed. Tests under `tests/conformance` fixtures `stdlib/logging` lock that combined surface. Mark L06 `done` only when those tests are green.

## Blocked by

None. L06.01 and L06.02 already land the per-class levels-filter and stdio-sink fixtures this parent row unifies.

## Non-goals

- **L06.01**: Leveled log (error/warn/info/debug); filter by level (already `done`)
- **L06.02**: Sink to stderr/stdout (string format) (already `done`)
- **H02**: Host stdio surface
- **L05**: In-language test framework
- structured JSON logs, file sinks, syslog, or a Node `console` identity surface

## Oracle checklist

- [x] O1: L06 leveled-logger and stdio-sink fixtures are locked by the stdlib logging conformance tests
  CHECK: cargo test -p draconic-conformance --test stdlib_logging
  EXPECT: test result: ok.
  EVIDENCE: met exit=0 match=yes sha256=8260863b4a06ec7a bytes=3140 at=2026-09-04T20:57:27.244Z

- [ ] O2: workspace tests stay green after the L06 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: unmet exit=timeout match=yes bytes=70857 at=2026-09-04T20:59:27.249Z
  ABANDON: leftover after --reverify; CHECK timed out at 120s (exit=timeout match=yes bytes=70857); home [[ticket-184-l06-workspace-timeout]]

## Pool

Durable links to task-pool ids. Never drop them.

- [[l06-logging-leveled-logger-stderr-stdout-sink]]

## See also

ROADMAP.md L06, `tests/conformance/tests/stdlib_logging.rs`, `tests/conformance/fixtures/stdlib/logging`, `crates/draconic-backend-llvm/src/es_logging.rs`, `crates/draconic-runtime/src/logging.rs`, CONTEXT.md, [[stdlib]], [[ticket-84-l06-logging-leveled-logger-stderr-stdout]], [[ticket-184-l06-workspace-timeout]].
