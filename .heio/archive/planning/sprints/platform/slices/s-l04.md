---
id: "s-l04"
title: "L04 Compression later: gzip/deflate byte buffers"
kind: slice
status: failed
sprint: "platform"
tags: []
created_at: "2026-09-02T12:47:51Z"
updated_at: "2026-09-04T20:02:50Z"
claimed-by: a14eb65d-6821-450c-8d7a-26efe720f4f7
---

# L04 Compression later: gzip/deflate byte buffers

## Why

ROADMAP L04 locks gzip/deflate compression and decompression of byte buffers on both targets (stdlib location: honest portable libs a simple service needs). This row is later than the L v1 bar (L01 + L03 + L05.01–02 + L06 + L08); the cut is still one atomic Loop so a Program can round-trip gzip and deflate buffers without leaving Draconic.

## Done

ROADMAP L04 is implemented test-first on both targets. A Program can gzip and deflate byte buffers (compress and decompress) through the designed stdlib compression surface, and invalid or truncated input errors rather than silently corrupting. Tests under `tests/conformance` fixtures `stdlib/compression` lock that surface. Mark L04 `done` only when those tests are green.

## Blocked by

None. The ticket names no dependency.

## Non-goals

- **L01**: Encoding UTF-8 / Base64 / hex
- **L03**: Crypto SHA-256 / CSPRNG
- **L09**: MIME multipart later
- **L10**: HMAC / AEAD later
- zip/tar archives, brotli, zstd, or a Node `zlib` identity surface
- changing the L v1 done bar to require L04

## Oracle checklist

- [x] O1: L04 gzip/deflate byte-buffer fixtures are locked by the stdlib compression conformance tests
  CHECK: cargo test -p draconic-conformance --test stdlib_compression
  EXPECT: test result: ok.
  EVIDENCE: met exit=0 match=yes sha256=9bd360f15e32c6a2 bytes=3138 at=2026-09-04T20:00:18.875Z

- [ ] O2: workspace tests stay green after the L04 Loop
  CHECK: cargo test --workspace
  EXPECT: test result: ok.
  EVIDENCE: unmet exit=timeout match=yes bytes=80616 at=2026-09-04T20:02:18.882Z
  ABANDON: leftover after --reverify; CHECK timed out at 120s (exit=timeout match=yes bytes=80616); home [[ticket-182-l04-workspace-timeout]]

## Pool

Durable links to task-pool ids. Never drop them.

- [[l04-compression-later-gzip-deflate]]

## See also

ROADMAP.md L04, `tests/conformance/tests/stdlib_compression.rs`, `tests/conformance/fixtures/stdlib/compression`, CONTEXT.md, [[stdlib]], [[ticket-82-l04-compression-later-gzip-deflate-byte]], [[ticket-182-l04-workspace-timeout]].
