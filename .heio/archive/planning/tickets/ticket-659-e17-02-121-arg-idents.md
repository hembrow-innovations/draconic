---
id: "ticket-659-e17-02-121-arg-idents"
title: "E17.02.121 claims arg idents hit object but fixture never runs them"
kind: ticket
status: closed
ticket_type: bug
labels: bug
tags: []
sprint: "platform"
created_at: "2026-09-07T03:16:17Z"
updated_at: "2026-09-08T12:00:00Z"
---
# E17.02.121 claims arg idents hit object but fixture never runs them

## Signal

Roadmap E17.02.121 is `done` for `with` plus Object.prototype accessor legacy. The Item text says object/method/arg idents hit object (else outer). The fixture never uses a with-object property as a call argument to `__defineGetter__` / `__defineSetter__` / `__lookupGetter__` / `__lookupSetter__`. The only argument identifier is unresolvable `e1702121_arg`. A skip of the object environment for args would still pass.

## Fit

Promoted into [[slice-669-e17-02-121-arg-idents]] in sprint [[platform]]. Executable: [[task-670-e17-02-121-arg-idents]]. Do not execute from this ticket. TrimLeft/trimRight arg idents stay on [[ticket-660-e17-02-120-arg-idents]].

## Notes

- **Roadmap ID**: E17.02.121. Targets: js. Tests: `tests/conformance` fixtures `es/legacy`.
- **Command**: `cargo test -p draconic-conformance --test legacy with_object_accessor_legacy` — 2 passed (`with_object_accessor_legacy_fixture_present`, `with_object_accessor_legacy_runs`). `draconic build --target js` on `with_object_accessor_legacy.drac` emits `with (` and `__defineGetter__` / `__defineSetter__` / `__lookupGetter__` / `__lookupSetter__`.
- **path:line**: `ROADMAP.md:222` (object/method/arg idents hit object). `tests/conformance/fixtures/es/legacy/with_object_accessor_legacy.drac:728` (`__defineGetter__(e1702121_arg, …)`). Contrast args-hit-object at `tests/conformance/fixtures/es/legacy/with_regexp_compile.drac:135`.

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Bug, not enhancement. E17.02.121 stays `done`. The only argument identifier is unresolvable `e1702121_arg`. Other calls use string literals.
- Confirmed: `cargo test -p draconic-conformance --test legacy with_object_accessor_legacy` prints `test result: ok.` Contrast `with_regexp_compile.drac:135` `argsHitObject`.
- Not already implemented. No prior rejection in `docs/`.
- Promoted into [[slice-669-e17-02-121-arg-idents]] with ready AFK [[task-670-e17-02-121-arg-idents]].
- Sibling [[ticket-660-e17-02-120-arg-idents]] stays separate.
