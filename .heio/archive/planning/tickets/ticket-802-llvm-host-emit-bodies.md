---
id: "ticket-802-llvm-host-emit-bodies"
title: "LLVM host emit bodies still per-domain walk_host"
kind: ticket
status: closed
ticket_type: observation
tags: []
created_at: "2026-09-11T22:30:00Z"
updated_at: "2026-09-11T09:20:00Z"
---

# LLVM host emit bodies still per-domain walk_host

## Signal

[[task-792-fold-host-catalog-emit]] folded host callee classification into one catalog helper (`lookup_host_api`). About 25 `walk_host_*` functions still each fingerprint a module and emit a full `@main`. Slice [[slice-784-llvm-no-fingerprint]] still needs those emit bodies on the one IR walker, without new fingerprint adapters.

## Fit

Promoted to [[slice-805-llvm-host-emit-bodies]] / [[task-806-llvm-host-emit-bodies]] in platform. Executable: [[task-806-llvm-host-emit-bodies]] (`completed`). Slice is `met`. Closed.

## Notes

- Classification lives in `crates/draconic-backend-llvm/src/host_catalog.rs`
- `try_folded_walks` host arms were not grown and were not deleted (that is [[task-793-delete-try-folded-walks]])
- Do not add a new host fingerprint adapter
- Keep Runtime C ABI calls

## Parent

[[slice-805-llvm-host-emit-bodies]]

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Classification already lives in `host_catalog` via `lookup_host_api`.
- [[slice-784-llvm-no-fingerprint]] is met; leftover is emit bodies, not a new fingerprint adapter.
- Frame-time search found `walk_host_*` definitions and no call sites in the llvm crate; the task confirms dead versus dispatched.
- dragons-audit is closed; new work is platform [[slice-805-llvm-host-emit-bodies]] / [[task-806-llvm-host-emit-bodies]].
- Do not absorb native console.log ([[ticket-773-native-console-log]]).
- Do not mark E17.02 or E18.44 done.
