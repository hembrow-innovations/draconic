---
id: "ticket-656-e17-02-129-const-for-head"
title: "E17.02.129 claims const for-head but fixture never runs it"
kind: ticket
status: closed
ticket_type: bug
labels: bug
tags: []
sprint: "platform"
created_at: "2026-09-07T02:26:45Z"
updated_at: "2026-09-08T12:00:00Z"
---
# E17.02.129 claims const for-head but fixture never runs it

## Signal

Roadmap E17.02.129 is `done` for `with` plus `var` in `for` heads. The Item text says `let`/`const` in head does not write object. The fixture only runs `for (let …)`. There is no `for (const …)` in that Program, and none under `tests/conformance/fixtures/es/legacy`.

## Fit

Promoted into [[slice-663-e17-02-129-const-for-head]] in sprint [[platform]]. Executable: [[task-664-e17-02-129-const-for-head]]. Do not execute from this ticket. Accessor get `this` stays on [[ticket-657-e17-02-129-accessor-get-this]].

## Notes

- **Roadmap ID**: E17.02.129. Targets: js. Tests: `tests/conformance` fixtures `es/legacy`.
- **Command**: `cargo test -p draconic-conformance --test legacy with_var_for` — 2 passed (`with_var_for_fixture_present`, `with_var_for_runs`). `draconic build --target js` on `with_var_for.drac` emits `with (` and `for (var` / `for (let`.
- **path:line**: `ROADMAP.md:214` (`let`/`const` in head). `tests/conformance/fixtures/es/legacy/with_var_for.drac:535` (`for (let k in …)`), `:546` (`for (let i = 0; …)`). No `for (const` in that file.

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Bug, not enhancement. E17.02.129 stays `done`. The fixture runs `for (let …)` only; no `for (const` under `es/legacy`.
- Not already implemented. No prior rejection in `docs/`.
- Promoted into [[slice-663-e17-02-129-const-for-head]] with ready AFK [[task-664-e17-02-129-const-for-head]].
- Sibling [[ticket-657-e17-02-129-accessor-get-this]] was left separate.
