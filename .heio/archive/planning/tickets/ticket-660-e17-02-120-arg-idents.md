---
id: "ticket-660-e17-02-120-arg-idents"
title: "E17.02.120 claims arg idents hit object but fixture never runs them"
kind: ticket
status: closed
ticket_type: bug
labels: bug
tags: []
sprint: "platform"
created_at: "2026-09-07T03:18:49Z"
updated_at: "2026-09-08T12:00:00Z"
---
# E17.02.120 claims arg idents hit object but fixture never runs them

## Signal

Roadmap E17.02.120 is `done` for `with` plus `String.prototype.trimLeft` / `trimRight`. The Item text says string/method/arg idents hit object (else outer). The fixture never uses a with-object property as a call argument to `trimLeft` / `trimRight`. The only argument identifier is unresolvable `e1702120_arg`. A skip of the object environment for args would still pass.

## Fit

Promoted into [[slice-671-e17-02-120-arg-idents]] in sprint [[platform]]. Executable: [[task-672-e17-02-120-arg-idents]]. Do not execute from this ticket. Accessor-legacy arg idents stay on [[ticket-659-e17-02-121-arg-idents]].

## Notes

- **Roadmap ID**: E17.02.120. Targets: js. Tests: `tests/conformance` fixtures `es/legacy`.
- **Command**: `cargo test -p draconic-conformance --test legacy with_string_trim_left_right` — 2 passed (`with_string_trim_left_right_fixture_present`, `with_string_trim_left_right_runs`). `draconic build --target js` on `with_string_trim_left_right.drac` emits `with (` and `trimLeft` / `trimRight`.
- **path:line**: `ROADMAP.md:223` (string/method/arg idents hit object). `tests/conformance/fixtures/es/legacy/with_string_trim_left_right.drac:590` (`trimLeft(e1702120_arg)`). Contrast args-hit-object at `tests/conformance/fixtures/es/legacy/with_regexp_compile.drac:135`.

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Bug, not enhancement. E17.02.120 stays `done`. The only argument identifier is unresolvable `e1702120_arg`. Other calls take no args.
- Confirmed: `cargo test -p draconic-conformance --test legacy with_string_trim_left_right` prints `test result: ok.` Contrast `with_regexp_compile.drac:135` `argsHitObject`.
- Not already implemented. No prior rejection in `docs/`.
- Promoted into [[slice-671-e17-02-120-arg-idents]] with ready AFK [[task-672-e17-02-120-arg-idents]].
- Sibling [[ticket-659-e17-02-121-arg-idents]] stays on its own slice.
