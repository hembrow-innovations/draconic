---
id: "ticket-658-e17-02-127-skipped-block"
title: "E17.02.127 claims skipped block not assigned through with but fixture
  only skips outside with"
kind: ticket
status: closed
ticket_type: bug
labels: bug
tags: []
sprint: "platform"
created_at: "2026-09-07T02:34:22Z"
updated_at: "2026-09-08T12:00:00Z"
---
# E17.02.127 claims skipped block not assigned through with but fixture only skips outside with

## Signal

Roadmap E17.02.127 is `done` for `with` plus Annex B.3.3 block-level function declarations. The Item text says skipped block not assigned through the object environment. Both skip tests wrap the entire `with` in `if (false)`, so the inner `{ function k(){…} }` is never skipped while a with object environment is on the stack.

## Fit

Promoted into [[slice-667-e17-02-127-skipped-block]] in sprint [[platform]]. Executable: [[task-668-e17-02-127-skipped-block]]. Do not execute from this ticket.

## Notes

- **Roadmap ID**: E17.02.127. Targets: js. Tests: `tests/conformance` fixtures `es/legacy`.
- **Command**: `cargo test -p draconic-conformance --test legacy with_block_function` — 2 passed (`with_block_function_fixture_present`, `with_block_function_runs`). `draconic build --target js` on `with_block_function.drac` emits `with (` and `{ function f()`.
- **path:line**: `ROADMAP.md:216` (skipped block not assigned). `tests/conformance/fixtures/es/legacy/with_block_function.drac:82` (`skippedBlockNotAssigned`), `:537` (`skippedOuterIfNotAssigned`, same body). Contrast inside-with skip at `tests/conformance/fixtures/es/legacy/with_if_function.drac:73`.

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Bug, not enhancement. E17.02.127 stays `done`. Both skip tests wrap the entire `with` in `if (false)`.
- Confirmed: `cargo test -p draconic-conformance --test legacy with_block_function` prints `test result: ok.` Contrast `with_if_function.drac:73`, which skips inside `with`.
- Not already implemented. No prior rejection in `docs/`.
- Promoted into [[slice-667-e17-02-127-skipped-block]] with ready AFK [[task-668-e17-02-127-skipped-block]].
