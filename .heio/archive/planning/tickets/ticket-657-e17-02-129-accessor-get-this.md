---
id: "ticket-657-e17-02-129-accessor-get-this"
title: "E17.02.129 claims accessor get this but fixture never reads"
kind: ticket
status: closed
ticket_type: bug
labels: bug
tags: []
sprint: "platform"
created_at: "2026-09-07T02:26:45Z"
updated_at: "2026-09-08T12:00:00Z"
---
# E17.02.129 claims accessor get this but fixture never reads

## Signal

Roadmap E17.02.129 is `done` for `with` plus `var` in `for` heads. The Item text says accessor get/set `this` is with object. The fixture only asserts setter `this`. Getters exist on those objects but are never invoked, so a wrong getter `this` would still pass.

## Fit

Promoted into [[slice-665-e17-02-129-accessor-get-this]] in sprint [[platform]]. Executable: [[task-666-e17-02-129-accessor-get-this]]. Do not execute from this ticket. Const for-heads stay on [[ticket-656-e17-02-129-const-for-head]].

## Notes

- **Roadmap ID**: E17.02.129. Targets: js. Tests: `tests/conformance` fixtures `es/legacy`.
- **Command**: `cargo test -p draconic-conformance --test legacy with_var_for` — 2 passed. `draconic build --target js` on `with_var_for.drac` emits `with (` and `for (var`.
- **path:line**: `ROADMAP.md:214` (accessor get/set `this`). `tests/conformance/fixtures/es/legacy/with_var_for.drac:264` (`forInAccessorSetThisIsWithObject`), `:491` (`annexBAccessorSetThisIsWithObject`), `:596` (`forOfAccessorSetThisIsWithObject`). Sibling get-this case: `with_var_decl.drac:173`.

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Bug, not enhancement. E17.02.129 stays `done`. Setter `this` is asserted; getters on those objects are never invoked.
- Confirmed: `cargo test -p draconic-conformance --test legacy with_var_for` prints `test result: ok.` Sibling `with_var_decl.drac:173` already reads a getter.
- Not already implemented for for-heads. No prior rejection in `docs/`.
- Promoted into [[slice-665-e17-02-129-accessor-get-this]] with ready AFK [[task-666-e17-02-129-accessor-get-this]].
- Sibling [[ticket-656-e17-02-129-const-for-head]] stays on its own slice.
