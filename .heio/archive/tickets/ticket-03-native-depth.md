---
id: "ticket-03-native-depth"
title: "Native depth (LLVM / Runtime)"
kind: ticket
status: closed
tags: []
created_at: "2026-07-26T00:00:00Z"
updated_at: "2026-07-26T00:00:00Z"
---

# Native depth (LLVM / Runtime)

Archived from `docs/planning/issues/closed/issues-3-native-depth.md`.

# Native depth (LLVM / Runtime)

## Description

Deepen the native path beyond fixture-green: GC quality, performance, richer stdlib, dual-worlds ergonomics, and real programs on LLVM—not just conformance stubs.

## Affected

- `crates/draconic-runtime`
- `crates/draconic-backend-llvm`
- `crates/draconic-embed`
- Dual-worlds boundary / native-only diagnostics

## Observed

N-cluster items are done with Runtime ABI + fixture observations. Production-shaped native binaries need durability and a usable std surface.

## Impact

Native target remains demo/fixture quality; hard to ship real portable+native Programs.

## Proposed Fix

1. Written gap list: GC, job queue, stdlib, Embed limits, dual-world UX, link/debug.
2. Prioritized backlog (Roadmap N-rows or issues) with measurable tests.
3. At least one improvement with benchmarks or stress tests.
4. Clear JS-only / native-only / portable policy (diagnostics, never silent wrong code).

## Human Brief

### Goal

Prioritize native-path depth beyond the existing N08 “real observations” backlog: what to do next after (or alongside) honesty-pass native debt.

### Why human

Multi-axis product call (GC vs perf vs stdlib vs dual-world UX vs packaging). Overlaps Roadmap **N08.*** — human must set priority so agents don’t invent a parallel backlog.

### Decisions needed

1. **Relation to N08:** drain N08 first | interleave one depth theme | treat N08 as orthogonal.
2. **First depth theme:** GC durability | job-queue stress | stdlib surface | Embed limits | dual-world diagnostics | link/debug.
3. **Success metric** for the first theme (benchmark, stress test, or demo Program).
4. Whether gaps become new Roadmap N-rows or vault issues.

### After decisions

- Written gap list for chosen themes.
- Prioritized atomic rows/issues with measurable tests.
- At least one improvement with benchmark or stress coverage.
- Restate JS-only / native-only / portable diagnostic policy if gaps found.

### Context (verified 2026-07-26)

- Honesty pass ([[issues-1-roadmap-honesty-pass]]) added **N08 / N08.01–N08.17** for real native ES observations off B08 hello stub.
- Runtime ABI seam and LLVM multipath honesty already closed under [[issues-6-architecture-cleanup]] children.
- Related: [[issues-5-ship-real-program]] (target choice will drive some depth).

### Out of scope

- Replacing N08 work with vague “make native better.”
- Silent wrong code on unsupported shapes (must hard-error).

## Comments

Related: [[issues-1-roadmap-honesty-pass]], [[issues-5-ship-real-program]]

> *This was generated by AI during triage.*
>
> **2026-07-26:** Category `enhancement`. Partially tracked already via N08*; residual depth (GC/stdlib/UX) still open. Priority/theme is judgment → `ready-for-human`. No out-of-scope match.

> **2026-07-26 (closed — decisions + seeds):**
>
> **Locked decisions:**
> 1. **N08.*** remains the primary native conformance drain — not replaced.
> 2. First depth theme: **GC durability / stress**.
> 3. Success metric: written gap list + Roadmap `todo` rows with measurable tests paths.
> 4. Gaps → new Roadmap **N09+** rows (not a parallel private backlog).
>
> **Deliverables:**
> - Gap list: [[native-depth-gaps]] (`docs/planning/native-depth-gaps.md`) — GC, job queue, stdlib, Embed limits, dual-world UX, link/debug; marks N06–N08 as already-on-Roadmap vs net-new; restates JS-only / native-only / portable hard-error policy.
> - Roadmap seeds: **N09**, **N09.01**, **N09.02** (`todo`, native, `crates/draconic-runtime` tests) after N08 block.
>
> No full GC rewrite; no Test262/README/examples/ADR/issues 2·4·5·7 edits. Planning + Roadmap only.
