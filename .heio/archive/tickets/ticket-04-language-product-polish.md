---
id: "ticket-04-language-product-polish"
title: "Language product polish"
kind: ticket
status: closed
tags: []
created_at: "2026-07-26T00:00:00Z"
updated_at: "2026-07-26T00:00:00Z"
---

# Language product polish

Archived from `docs/planning/issues/closed/issues-4-language-product-polish.md`.

# Language product polish

## Description

Make the toolchain presentable: README/status, docs, examples, CLI UX, diagnostic quality, optional playground/REPL.

## Affected

- `README.md` (still says bootstrap in progress)
- `crates/draconic-cli`, diagnostics
- Examples / onboarding path

## Observed

Roadmap complete while product docs lag; onboarding path unclear.

## Impact

Hard for humans (and agents) to use Draconic as a language product rather than a Loop checklist.

## Proposed Fix

1. README status, build, CLI examples match reality.
2. Minimal write→parse→build js|native path documented.
3. In-repo examples (portable + dual-worlds).
4. Diagnostics spot-check on common mistakes.
5. Decision: REPL/playground now / later / never.

## Human Brief

### Goal

Decide product-facing polish scope (especially REPL/playground) and order of README / examples / diagnostics work so agents can execute slices.

### Why human

REPL/playground now|later|never is product judgment. README “status” wording should match how you want the language described publicly.

### Decisions needed

1. **REPL / playground:** now | later | never.
2. **Public status line** for README (honest Loop/completeness vs “usable toolchain”).
3. **Examples home:** `examples/` vs `apps/` vs docs-only; portable-only first or dual-worlds required.
4. Priority order among: README truth, clone→build→run path, diagnostics spot-check, examples.

### After decisions

Split ready-for-agent units, e.g.:

- README status + build + CLI (`parse` / `build --target js|native`) match reality.
- Minimal write→parse→build path documented.
- In-repo examples per decided layout.
- Diagnostics spot-check on common mistakes (if prioritized).

### Context (verified 2026-07-26)

- README still says “Bootstrap in progress.”
- CLI has parse/build/test; layout table is largely current (frontend, linker present).
- No first-class examples tree for onboarding.
- Related: [[issues-5-ship-real-program]] may supply the flagship example.

### Out of scope

- Full website/marketing site.
- Changing language semantics for prettier errors without a separate issue.

## Comments

> *This was generated by AI during triage.*
>
> **2026-07-26:** Category `enhancement`. Confirmed README lag and weak onboarding path. REPL decision + public status wording → `ready-for-human`. No out-of-scope match. Note: Observed “Roadmap complete” is stale post-honesty-pass (~21 `todo`s remain).

> **2026-07-26 decisions (locked):**
>
> 1. **REPL/playground:** **later** — do not build now.
> 2. **Public status:** honest — bootstrap/Roadmap Loop complete for spine; language completeness ongoing via ROADMAP todos; toolchain usable for parse/build/test.
> 3. **Examples home:** `examples/` (sibling [[issues-5-ship-real-program]]).
> 4. **Priority:** README truth + clone→build→run docs first. Diagnostics spot-check deferred.

> **2026-07-26 implement:** README rewritten (Status, Build, Quick start write→parse→build js|native, CLI matches `draconic help`, Layout). No REPL. No `docs/reference/guides/getting-started.md` (README covers onboarding; guides stay engineering-ops). Issue closed.
