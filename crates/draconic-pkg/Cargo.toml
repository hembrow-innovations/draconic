[package]
name = "draconic-pkg"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Draconic package manifests, lockfiles, and module cache (Roadmap K)"

[dependencies]
toml.workspace = true
