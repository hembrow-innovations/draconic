type Point = { x: number; y: number };
let p: Point = { x: 1, y: 2 };
let q: { x: number } = p;
let n: number = p.x;
let m: number = q.x;
function take(o: { x: number }): number {
  return o.x;
}
let r: number = take(p);
