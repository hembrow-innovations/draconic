let twm = typeof WeakMap;
let tws = typeof WeakSet;
let gwm = globalThis.WeakMap === WeakMap;
let gws = globalThis.WeakSet === WeakSet;
let k1 = {};
let k2 = {};
let wm = new WeakMap();
let wmSet = wm.set(k1, 1);
let wmGet = wm.get(k1);
let wmHas = wm.has(k1);
let wmMiss = wm.has(k2);
wm.set(k2, "two");
let wmGet2 = wm.get(k2);
let wmDel = wm.delete(k1);
let wmHasAfter = wm.has(k1);
let wmGetMiss = wm.get(k1);
let ws = new WeakSet();
let wsAdd = ws.add(k1);
let wsHas = ws.has(k1);
let wsMiss = ws.has(k2);
ws.add(k1);
let wsHas2 = ws.has(k1);
ws.add(k2);
let wsHas3 = ws.has(k2);
let wsDel = ws.delete(k1);
let wsHasAfter = ws.has(k1);
