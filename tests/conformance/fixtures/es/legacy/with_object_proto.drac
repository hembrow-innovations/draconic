// E17.02.116: with + Object.prototype.__proto__ (Annex B.2.2 / B.3.1:
// get/set prototype; object literal __proto__ vs computed ["__proto__"])
// through object environment (non-strict). Object/base/value idents hit
// object (else outer); __proto__ ident hits object (else outer, typically
// Object.prototype accessor); with (obj) __proto__ ident is the prototype
// accessor (this is with object); inherited HasBinding; accessor this is
// with object; unresolvable → ReferenceError; live close; nested with;
// unscopables skip.

function memberGetHitsObject() {
  let p = {};
  let env = { p: p };
  with (env) {
    return p.__proto__ === Object.prototype;
  }
}
let a = memberGetHitsObject();

function memberGetMissUsesOuter() {
  let p = {};
  with ({}) {
    return p.__proto__ === Object.prototype;
  }
}
let b = memberGetMissUsesOuter();

function memberSetHitsObject() {
  let base = { k: 9 };
  let child = { m: 1 };
  let env = { child: child, base: base };
  with (env) {
    child.__proto__ = base;
  }
  return child.k === 9 && child.m === 1 && child.__proto__ === base;
}
let c = memberSetHitsObject();

function memberSetMissUsesOuter() {
  let base = { k: 8 };
  let child = { m: 1 };
  with ({}) {
    child.__proto__ = base;
  }
  return child.k === 8 && child.m === 1 && child.__proto__ === base;
}
let d = memberSetMissUsesOuter();

function memberSetReadBack() {
  let base = { k: 7 };
  let child = {};
  let env = { child: child, base: base };
  let ok;
  with (env) {
    child.__proto__ = base;
    ok = child.__proto__ === base && child.k === 7;
  }
  return ok === true;
}
let e = memberSetReadBack();

function litProtoHitsObject() {
  let outer = { k: 0 };
  let env = { proto: { k: 9 } };
  let o;
  with (env) {
    o = { __proto__: proto, y: 2 };
  }
  return (
    o.k === 9 &&
    o.y === 2 &&
    o.__proto__ === env.proto &&
    Object.prototype.hasOwnProperty.call(o, "__proto__") === false
  );
}
let f = litProtoHitsObject();

function litProtoMissUsesOuter() {
  let proto = { k: 4 };
  let o;
  with ({}) {
    o = { __proto__: proto, y: 3 };
  }
  return o.k === 4 && o.y === 3 && o.__proto__ === proto;
}
let g = litProtoMissUsesOuter();

function litProtoNotOwn() {
  let env = { proto: { k: 1 } };
  let o;
  with (env) {
    o = { __proto__: proto };
  }
  return Object.prototype.hasOwnProperty.call(o, "__proto__") === false;
}
let h = litProtoNotOwn();

function computedProtoOwn() {
  let env = { proto: { k: 9 } };
  let o;
  with (env) {
    o = { ["__proto__"]: proto };
  }
  return Object.prototype.hasOwnProperty.call(o, "__proto__") === true;
}
let j = computedProtoOwn();

function computedProtoNotSetPrototype() {
  let env = { proto: { k: 9 } };
  let o;
  with (env) {
    o = { ["__proto__"]: proto };
  }
  return Object.getPrototypeOf(o) === Object.prototype;
}
let k = computedProtoNotSetPrototype();

function computedGetEqualsOwn() {
  let env = { proto: { k: 9 } };
  let o;
  with (env) {
    o = { ["__proto__"]: proto };
  }
  return o.__proto__ === env.proto;
}
let l = computedGetEqualsOwn();

function protoIdentGetOrdinary() {
  let obj = {};
  with (obj) {
    return __proto__ === Object.prototype;
  }
}
let m = protoIdentGetOrdinary();

function protoIdentSetOrdinary() {
  let base = { k: 9 };
  let child = { m: 1 };
  with (child) {
    __proto__ = base;
  }
  return child.k === 9 && child.m === 1 && child.__proto__ === base;
}
let n = protoIdentSetOrdinary();

function protoIdentHitsOverOuter() {
  let __proto__ = 11;
  let obj = { k: 1 };
  with (obj) {
    return __proto__ === Object.prototype && __proto__ !== 11;
  }
}
let p = protoIdentHitsOverOuter();

function protoIdentMissNullProtoUsesOuter() {
  let __proto__ = 5;
  with (Object.create(null)) {
    return __proto__ === 5;
  }
}
let q = protoIdentMissNullProtoUsesOuter();

function protoIdentOwnDataShadows() {
  let obj = {};
  Object.defineProperty(obj, "__proto__", {
    value: 7,
    configurable: true,
    writable: true,
    enumerable: true
  });
  with (obj) {
    return __proto__ === 7;
  }
}
let r = protoIdentOwnDataShadows();

function inheritedHasBinding() {
  let proto = {};
  let obj = Object.create(proto);
  with (obj) {
    return __proto__ === proto;
  }
}
let s = inheritedHasBinding();

function accessorThisIsWithObject() {
  let seen = null;
  let orig = Object.getOwnPropertyDescriptor(Object.prototype, "__proto__");
  try {
    Object.defineProperty(Object.prototype, "__proto__", {
      get: function () {
        seen = this;
        return orig.get.call(this);
      },
      set: orig.set,
      configurable: true,
      enumerable: orig.enumerable
    });
    let obj = { k: 1 };
    let ok;
    with (obj) {
      ok = __proto__ === Object.prototype && seen === obj;
    }
    return ok;
  } finally {
    Object.defineProperty(Object.prototype, "__proto__", orig);
  }
}
let t = accessorThisIsWithObject();

function unresolvableThrows() {
  with ({}) {
    try {
      e1702116_noglob.__proto__;
      return false;
    } catch (err) {
      return (
        err instanceof ReferenceError &&
        typeof globalThis.e1702116_noglob === "undefined"
      );
    }
  }
}
let u = unresolvableThrows();

function liveClose() {
  let obj = { k: 1 };
  let run;
  with (obj) {
    run = function () {
      return __proto__;
    };
  }
  let base = { z: 1 };
  obj.__proto__ = base;
  return run() === base;
}
let v = liveClose();

function nestedInnerWins() {
  let inner = { k: 1 };
  let outer = { k: 2 };
  let base = { z: 9 };
  inner.__proto__ = base;
  with (outer) {
    with (inner) {
      return __proto__ === base && k === 1;
    }
  }
}
let w = nestedInnerWins();

function nestedInnerMissOuter() {
  let outer = { k: 2 };
  with (outer) {
    with (Object.create(null)) {
      return k === 2;
    }
  }
}
let aa = nestedInnerMissOuter();

function unscopablesUsesOuter() {
  let __proto__ = 11;
  let obj = { k: 1 };
  let u = Object.create(null);
  u["__proto__"] = true;
  obj[Symbol.unscopables] = u;
  with (obj) {
    return __proto__ === 11;
  }
}
let ab = unscopablesUsesOuter();

function unscopablesFalseyDoesNotBlock() {
  let __proto__ = 11;
  let obj = { k: 1 };
  let u = Object.create(null);
  u["__proto__"] = false;
  obj[Symbol.unscopables] = u;
  with (obj) {
    return __proto__ === Object.prototype;
  }
}
let ac = unscopablesFalseyDoesNotBlock();

function memberGetEmpty() {
  let env = { p: {} };
  with (env) {
    return p.__proto__ === Object.prototype;
  }
}
let ad = memberGetEmpty();

function memberSetNull() {
  let child = { m: 1 };
  let env = { child: child };
  with (env) {
    child.__proto__ = null;
  }
  return Object.getPrototypeOf(child) === null && child.m === 1;
}
let ae = memberSetNull();

function litProtoAndY() {
  let env = { proto: { k: 9 } };
  let o;
  with (env) {
    o = { __proto__: proto, y: 2 };
  }
  return o.k === 9 && o.y === 2 && o.__proto__ === env.proto;
}
let af = litProtoAndY();

function bracketGetHitsObject() {
  let p = {};
  let env = { p: p };
  with (env) {
    return p["__proto__"] === Object.prototype;
  }
}
let ag = bracketGetHitsObject();

function bracketSetHitsObject() {
  let base = { k: 6 };
  let child = { m: 1 };
  let env = { child: child, base: base };
  with (env) {
    child["__proto__"] = base;
  }
  return child.k === 6 && child.__proto__ === base;
}
let ah = bracketSetHitsObject();

function bothSidesAlwaysEvaled() {
  let n = 0;
  let base = { k: 1 };
  let child = {};
  let env = {
    get child() {
      n = n + 1;
      return child;
    },
    get base() {
      n = n + 1;
      return base;
    }
  };
  with (env) {
    child.__proto__ = base;
  }
  return n === 2 && child.__proto__ === base && child.k === 1;
}
let aj = bothSidesAlwaysEvaled();

function unresolvableBase() {
  with ({}) {
    try {
      e1702116_base.__proto__ = {};
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let ak = unresolvableBase();

function liveCloseMember() {
  let env = { p: { m: 1 } };
  let run;
  with (env) {
    run = function () {
      return p.__proto__;
    };
  }
  let base = { z: 2 };
  env.p.__proto__ = base;
  return run() === base;
}
let al = liveCloseMember();

function nestedLitInnerWins() {
  let inner = { proto: { k: 5 } };
  let outer = { proto: { k: 1 } };
  let o;
  with (outer) {
    with (inner) {
      o = { __proto__: proto };
    }
  }
  return o.k === 5 && o.__proto__ === inner.proto;
}
let am = nestedLitInnerWins();

function nestedLitMissOuter() {
  let outer = { proto: { k: 3 } };
  let o;
  with (outer) {
    with ({}) {
      o = { __proto__: proto };
    }
  }
  return o.k === 3 && o.__proto__ === outer.proto;
}
let an = nestedLitMissOuter();

function getPrototypeOfIdentity() {
  let p = {};
  let env = { p: p };
  with (env) {
    return p.__proto__ === Object.getPrototypeOf(p);
  }
}
let ap = getPrototypeOfIdentity();

function hasOwnLitVsComputed() {
  let env = { proto: {} };
  let lit;
  let comp;
  with (env) {
    lit = { __proto__: proto };
    comp = { ["__proto__"]: proto };
  }
  return (
    Object.prototype.hasOwnProperty.call(lit, "__proto__") === false &&
    Object.prototype.hasOwnProperty.call(comp, "__proto__") === true
  );
}
let aq = hasOwnLitVsComputed();

function inheritAfterIdentSet() {
  let base = { k: 9 };
  let child = { m: 1 };
  with (child) {
    __proto__ = base;
    return k === 9 && m === 1;
  }
}
let ar = inheritAfterIdentSet();

function memberSetYieldsRhs() {
  let base = { k: 1 };
  let child = {};
  let env = { child: child, base: base };
  let r;
  with (env) {
    r = child.__proto__ = base;
  }
  return r === base;
}
let at = memberSetYieldsRhs();

function protoIdentAssignYieldsRhs() {
  let base = { k: 1 };
  let child = {};
  let r;
  with (child) {
    r = __proto__ = base;
  }
  return r === base && child.__proto__ === base;
}
let au = protoIdentAssignYieldsRhs();

function getPrototypeOfAfterSet() {
  let base = { k: 2 };
  let child = {};
  let env = { child: child, base: base };
  with (env) {
    child.__proto__ = base;
  }
  return Object.getPrototypeOf(child) === base;
}
let av = getPrototypeOfAfterSet();

function nestedUnscopables() {
  let __proto__ = 11;
  let inner = { k: 1 };
  let u = Object.create(null);
  u["__proto__"] = true;
  inner[Symbol.unscopables] = u;
  let outer = Object.create(null);
  with (outer) {
    with (inner) {
      return __proto__ === 11;
    }
  }
}
let aw = nestedUnscopables();

function getterRunsOnce() {
  let c = 0;
  let p = {};
  let env = {
    get p() {
      c = c + 1;
      return p;
    }
  };
  let out;
  with (env) {
    out = p.__proto__;
  }
  return out === Object.prototype && c === 1;
}
let ax = getterRunsOnce();

function litEmptyIsObjectPrototype() {
  let env = { x: 1 };
  let o;
  with (env) {
    o = {};
  }
  return o.__proto__ === Object.prototype;
}
let ay = litEmptyIsObjectPrototype();

function childMPreserved() {
  let base = { k: 9 };
  let child = { m: 1 };
  let env = { child: child, base: base };
  with (env) {
    child.__proto__ = base;
  }
  return child.m === 1 && child.k === 9;
}
let az = childMPreserved();

function liveCloseAfterMutateIdent() {
  let obj = {};
  let run;
  with (obj) {
    run = function () {
      return __proto__;
    };
  }
  let base = { z: 3 };
  obj.__proto__ = base;
  return run() === base && run().z === 3;
}
let ba = liveCloseAfterMutateIdent();

function computedKeyIdentHitsObject() {
  let k = "outer";
  let env = { k: "__proto__", proto: { n: 4 } };
  let o;
  with (env) {
    o = { [k]: proto };
  }
  return (
    Object.prototype.hasOwnProperty.call(o, "__proto__") === true &&
    o.__proto__ === env.proto &&
    Object.getPrototypeOf(o) === Object.prototype
  );
}
let bb = computedKeyIdentHitsObject();

function unresolvableLitValue() {
  let ran = false;
  let env = {
    get marker() {
      ran = true;
      return 1;
    }
  };
  with (env) {
    try {
      ({ __proto__: e1702116_lit });
      return false;
    } catch (err) {
      return err instanceof ReferenceError && ran === false;
    }
  }
}
let bc = unresolvableLitValue();

function memberMissStillOuterP() {
  let p = {};
  with ({ x: 1 }) {
    return p.__proto__ === Object.prototype;
  }
}
let bd = memberMissStillOuterP();

function defineOwnThenIdent() {
  let obj = {};
  Object.defineProperty(obj, "__proto__", {
    value: { own: 1 },
    configurable: true,
    writable: true,
    enumerable: true
  });
  with (obj) {
    return __proto__.own === 1 && Object.getPrototypeOf(obj) === Object.prototype;
  }
}
let be = defineOwnThenIdent();

function identSetThenGetPrototypeOf() {
  let base = { k: 2 };
  let child = {};
  with (child) {
    __proto__ = base;
  }
  return Object.getPrototypeOf(child) === base && child.k === 2;
}
let bf = identSetThenGetPrototypeOf();

function litProtoNull() {
  let env = { proto: null };
  let o;
  with (env) {
    o = { __proto__: proto, y: 1 };
  }
  return Object.getPrototypeOf(o) === null && o.y === 1;
}
let bg = litProtoNull();

function identSetNull() {
  let child = { m: 1 };
  with (child) {
    __proto__ = null;
  }
  return Object.getPrototypeOf(child) === null && child.m === 1;
}
let bh = identSetNull();

function bracketKeyIdentHitsObject() {
  let key = "nope";
  let p = {};
  let env = { p: p, key: "__proto__" };
  with (env) {
    return p[key] === Object.prototype;
  }
}
let bi = bracketKeyIdentHitsObject();

function nestedProtoIdentInnerWins() {
  let inner = {};
  let base = { z: 1 };
  inner.__proto__ = base;
  with ({}) {
    with (inner) {
      return __proto__ === base;
    }
  }
}
let bj = nestedProtoIdentInnerWins();

function nestedProtoIdentMissOuter() {
  let outer = {};
  with (outer) {
    with (Object.create(null)) {
      return __proto__ === Object.prototype;
    }
  }
}
let bk = nestedProtoIdentMissOuter();

function objectPrototypeIdentity() {
  let p = {};
  let env = { p: p, Object: Object };
  with (env) {
    return p.__proto__ === Object.prototype;
  }
}
let bl = objectPrototypeIdentity();

function objectIdentHitsObject() {
  let env = {
    Object: { prototype: { fake: 1 } },
    p: {}
  };
  with (env) {
    return p.__proto__ !== Object.prototype && Object.prototype.fake === 1;
  }
}
let bm = objectIdentHitsObject();

function protoIdentOnObjectPrototype() {
  with (Object.prototype) {
    return __proto__ === null || __proto__ === Object.prototype.__proto__;
  }
}
let bn = protoIdentOnObjectPrototype();

function liveCloseLitBinding() {
  let env = { proto: { k: 1 } };
  let run;
  with (env) {
    run = function () {
      return { __proto__: proto };
    };
  }
  env.proto = { k: 2 };
  let o = run();
  return o.k === 2 && o.__proto__ === env.proto;
}
let bp = liveCloseLitBinding();

function unscopablesSecondIdent() {
  let proto = { k: 8 };
  let env = { proto: { k: 1 }, skip: 1 };
  let u = Object.create(null);
  u.skip = true;
  env[Symbol.unscopables] = u;
  let o;
  with (env) {
    o = { __proto__: proto };
  }
  return o.k === 1 && o.__proto__ === env.proto;
}
let bq = unscopablesSecondIdent();

function inheritedGetterThis() {
  let seen = null;
  let orig = Object.getOwnPropertyDescriptor(Object.prototype, "__proto__");
  try {
    Object.defineProperty(Object.prototype, "__proto__", {
      get: function () {
        seen = this;
        return orig.get.call(this);
      },
      set: orig.set,
      configurable: true,
      enumerable: orig.enumerable
    });
    let proto = {};
    let obj = Object.create(proto);
    let ok;
    with (obj) {
      ok = __proto__ === proto && seen === obj;
    }
    return ok;
  } finally {
    Object.defineProperty(Object.prototype, "__proto__", orig);
  }
}
let br = inheritedGetterThis();

function unresolvableRhsAfterBase() {
  let ran = false;
  let child = {};
  let env = {
    get child() {
      ran = true;
      return child;
    }
  };
  with (env) {
    try {
      child.__proto__ = e1702116_rhs;
      return false;
    } catch (err) {
      return err instanceof ReferenceError && ran === true;
    }
  }
}
let bs = unresolvableRhsAfterBase();

function unresolvableCalleeSkipsArg() {
  let ran = false;
  let env = {
    get proto() {
      ran = true;
      return {};
    }
  };
  with (env) {
    try {
      e1702116_first.__proto__ = proto;
      return false;
    } catch (err) {
      return err instanceof ReferenceError && ran === false;
    }
  }
}
let bt = unresolvableCalleeSkipsArg();

function emptyMissUsesOuterProtoIdent() {
  let __proto__ = 4;
  with (Object.create(null)) {
    return __proto__ === 4;
  }
}
let bu = emptyMissUsesOuterProtoIdent();

function withObjectCreateProto() {
  let base = { k: 9 };
  let child = Object.create(base);
  let env = { child: child };
  with (env) {
    return child.__proto__ === base && child.k === 9;
  }
}
let bv = withObjectCreateProto();

function computedAndLitTogether() {
  let env = { proto: { k: 9 }, extra: { k: 1 } };
  let lit;
  let comp;
  with (env) {
    lit = { __proto__: proto, y: 2 };
    comp = { ["__proto__"]: extra, y: 3 };
  }
  return (
    lit.k === 9 &&
    lit.y === 2 &&
    Object.getPrototypeOf(lit) === env.proto &&
    Object.prototype.hasOwnProperty.call(comp, "__proto__") === true &&
    comp.__proto__ === env.extra &&
    Object.getPrototypeOf(comp) === Object.prototype &&
    comp.y === 3
  );
}
let bw = computedAndLitTogether();

function protoIdentAssignCreatesChain() {
  let a = { x: 1 };
  let b = { y: 2 };
  with (b) {
    __proto__ = a;
  }
  return b.x === 1 && b.y === 2 && b.__proto__ === a;
}
let bx = protoIdentAssignCreatesChain();

function memberGetMissingIsUndefined() {
  let p = Object.create(null);
  let env = { p: p };
  with (env) {
    return p.__proto__ === undefined;
  }
}
let by = memberGetMissingIsUndefined();

function identOnNullProtoUnresolvableVsOuter() {
  let obj = Object.create(null);
  with (obj) {
    try {
      e1702116_nullmiss;
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let bz = identOnNullProtoUnresolvableVsOuter();

function setterThisIsWithObject() {
  let seen = null;
  let orig = Object.getOwnPropertyDescriptor(Object.prototype, "__proto__");
  try {
    Object.defineProperty(Object.prototype, "__proto__", {
      get: orig.get,
      set: function (v) {
        seen = this;
        return orig.set.call(this, v);
      },
      configurable: true,
      enumerable: orig.enumerable
    });
    let child = {};
    let base = { k: 1 };
    with (child) {
      __proto__ = base;
    }
    return seen === child && child.__proto__ === base && child.k === 1;
  } finally {
    Object.defineProperty(Object.prototype, "__proto__", orig);
  }
}
let ca = setterThisIsWithObject();

function objectIdentMissUsesOuter() {
  let p = {};
  with ({}) {
    return p.__proto__ === Object.prototype && Object.prototype.hasOwnProperty.call(p, "__proto__") === false;
  }
}
let cb = objectIdentMissUsesOuter();

function litProtoGrouping() {
  let env = { proto: { k: 5 } };
  let o;
  with (env) {
    o = { __proto__: proto };
  }
  return o.__proto__ === env.proto && Object.getPrototypeOf(o) === env.proto;
}
let cc = litProtoGrouping();

function nestedWithUnscopablesInnerSkipOuterHit() {
  let inner = { k: 1 };
  let u = Object.create(null);
  u["__proto__"] = true;
  inner[Symbol.unscopables] = u;
  let outer = {};
  with (outer) {
    with (inner) {
      return __proto__ === Object.prototype;
    }
  }
}
let cd = nestedWithUnscopablesInnerSkipOuterHit();

function liveCloseOwnData() {
  let obj = {};
  Object.defineProperty(obj, "__proto__", {
    value: 1,
    configurable: true,
    writable: true,
    enumerable: true
  });
  let run;
  with (obj) {
    run = function () {
      return __proto__;
    };
  }
  Object.defineProperty(obj, "__proto__", {
    value: 2,
    configurable: true,
    writable: true,
    enumerable: true
  });
  return run() === 2;
}
let ce = liveCloseOwnData();

function cafeChainThroughWith() {
  let base = { k: 9 };
  let child = { m: 1 };
  let env = { child: child, base: base };
  with (env) {
    child.__proto__ = base;
    return child.k === 9 && child.m === 1 && child.__proto__ === base && Object.getPrototypeOf(child) === base;
  }
}
let cf = cafeChainThroughWith();
