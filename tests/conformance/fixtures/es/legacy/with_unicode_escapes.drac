// E17.02.76: with + Unicode escapes (`\xHH`, `\uXXXX`, `\u{X…}` in
// string/template literals; cooked; well-formed scalar values) through
// object environment (non-strict). Concat/interpolation idents hit object
// (else outer); empty/`""`; nested; inherited HasBinding; accessor get this
// is with object; unresolvable concat/interp → ReferenceError; live close;
// nested with; unscopables skip.

function concatHexHitsObject() {
  let x = "out";
  let obj = { x: "in" };
  let out;
  with (obj) {
    out = "\x41" + x;
  }
  return out === "Ain" && x === "out";
}
let a = concatHexHitsObject();

function concatU4HitsObject() {
  let x = "out";
  let obj = { x: "in" };
  let out;
  with (obj) {
    out = x + "\u0042";
  }
  return out === "inB" && x === "out";
}
let b = concatU4HitsObject();

function concatCpHitsObject() {
  let x = "out";
  let obj = { x: "in" };
  let out;
  with (obj) {
    out = "\u{43}" + x;
  }
  return out === "Cin" && x === "out";
}
let c = concatCpHitsObject();

function concatMissUsesOuter() {
  let x = "out";
  let obj = {};
  let out;
  with (obj) {
    out = "\x41" + x;
  }
  return out === "Aout";
}
let d = concatMissUsesOuter();

function concatBothSides() {
  let obj = { x: "\x41", y: "\u0042" };
  let out;
  with (obj) {
    out = x + y;
  }
  return out === "AB";
}
let e = concatBothSides();

function mixedObjectOuter() {
  let y = "\u{59}";
  let obj = { x: "\x58" };
  let out;
  with (obj) {
    out = x + y;
  }
  return out === "XY" && y === "Y";
}
let f = mixedObjectOuter();

function templateHexInterp() {
  let obj = { x: "in" };
  let out;
  with (obj) {
    out = `\x48${x}`;
  }
  return out === "Hin";
}
let g = templateHexInterp();

function templateU4Interp() {
  let obj = { x: "K" };
  let out;
  with (obj) {
    out = `\u004F${x}`;
  }
  return out === "OK";
}
let h = templateU4Interp();

function templateCpInterp() {
  let obj = { x: "!" };
  let out;
  with (obj) {
    out = `\u{41}${x}`;
  }
  return out === "A!";
}
let i = templateCpInterp();

function templateEmojiInterp() {
  let obj = { x: "z" };
  let out;
  with (obj) {
    out = `\u{1F600}${x}`;
  }
  return out === "\u{1F600}z" && out.length === 3;
}
let j = templateEmojiInterp();

function templateMixCookedAndLit() {
  let obj = { z: "Z" };
  let out;
  with (obj) {
    out = `x${"\u0041"}y${z}`;
  }
  return out === "xAyZ";
}
let k = templateMixCookedAndLit();

function emptyString() {
  let obj = { x: "in" };
  let out;
  with (obj) {
    out = "" + "\u{0}" + "";
  }
  return out.length === 1 && out.charCodeAt(0) === 0 && obj.x === "in";
}
let l = emptyString();

function emptyTemplate() {
  let obj = { x: "in" };
  let out;
  with (obj) {
    out = ``;
  }
  return out === "" && obj.x === "in";
}
let m = emptyTemplate();

function nestedConcat() {
  let obj = { x: "in", y: "Y" };
  let out;
  with (obj) {
    out = "\x41" + ("\u0042" + x) + y;
  }
  return out === "ABinY";
}
let n = nestedConcat();

function nestedTemplate() {
  let obj = { x: "in", y: "Y" };
  let out;
  with (obj) {
    out = `outer ${`\u{69}nner ${x}`} ${y}`;
  }
  return out === "outer inner in Y";
}
let p = nestedTemplate();

function hexNullConcat() {
  let obj = { x: "z" };
  let out;
  with (obj) {
    out = "\x00" + x;
  }
  return out.length === 2 && out.charCodeAt(0) === 0 && out[1] === "z";
}
let q = hexNullConcat();

function eqHexToIdent() {
  let obj = { x: "A" };
  let out;
  with (obj) {
    out = "\x41" === x;
  }
  return out === true;
}
let r = eqHexToIdent();

function eqU4ToIdent() {
  let obj = { x: "A" };
  let out;
  with (obj) {
    out = "\u0041" === x;
  }
  return out === true;
}
let s = eqU4ToIdent();

function eqCpToIdent() {
  let obj = { x: "A" };
  let out;
  with (obj) {
    out = "\u{41}" === x;
  }
  return out === true;
}
let t = eqCpToIdent();

function emojiLengthViaIdent() {
  let obj = { s: "\u{1F600}" };
  let len;
  let c0;
  with (obj) {
    len = s.length;
    c0 = s[0];
  }
  return len === 2 && typeof c0 === "string";
}
let u = emojiLengthViaIdent();

function identEscapeHitsObject() {
  let x = "out";
  let obj = { x: "in" };
  let out;
  with (obj) {
    out = "\x21" + \u0078;
  }
  return out === "!in" && x === "out";
}
let v = identEscapeHitsObject();

function identEscapeCpHitsObject() {
  let x = "out";
  let obj = { x: "in" };
  let out;
  with (obj) {
    out = \u{78} + "\u0021";
  }
  return out === "in!" && x === "out";
}
let w = identEscapeCpHitsObject();

function identEscapeMissUsesOuter() {
  let x = "out";
  let obj = {};
  let out;
  with (obj) {
    out = "\x41" + \u0078;
  }
  return out === "Aout";
}
let aa = identEscapeMissUsesOuter();

function identEscapeInTemplate() {
  let x = "out";
  let obj = { x: "in" };
  let out;
  with (obj) {
    out = `!\u0041${\u0078}!`;
  }
  return out === "!Ain!" && x === "out";
}
let ab = identEscapeInTemplate();

function unscopablesSkip() {
  let x = "out";
  let obj = { x: "in" };
  obj[Symbol.unscopables] = { x: true };
  let out;
  with (obj) {
    out = "\x41" + x;
  }
  return out === "Aout" && obj.x === "in";
}
let ac = unscopablesSkip();

function inheritedHasBinding() {
  let proto = { x: "in" };
  let obj = Object.create(proto);
  let out;
  with (obj) {
    out = "\u0041" + x;
  }
  return out === "Ain";
}
let ad = inheritedHasBinding();

function accessorGetThis() {
  let seen = null;
  let obj = {
    get x() {
      seen = this;
      return "in";
    }
  };
  let out;
  with (obj) {
    out = "\x41" + x;
  }
  return out === "Ain" && seen === obj;
}
let ae = accessorGetThis();

function unresolvableThrows() {
  with ({}) {
    try {
      "\x41" + e170276_missing;
      return false;
    } catch (err) {
      return (
        err instanceof ReferenceError &&
        typeof globalThis.e170276_missing === "undefined"
      );
    }
  }
}
let af = unresolvableThrows();

function unresolvableSkipsLater() {
  let ran = false;
  let obj = {
    get y() {
      ran = true;
      return "y";
    }
  };
  with (obj) {
    try {
      "\u0041" + e170276_first + y;
      return false;
    } catch (err) {
      return err instanceof ReferenceError && ran === false;
    }
  }
}
let ag = unresolvableSkipsLater();

function liveClose() {
  let obj = { x: "a" };
  let run;
  with (obj) {
    run = function () {
      return "\x21" + x + "\u0021";
    };
  }
  obj.x = "b";
  return run() === "!b!";
}
let ah = liveClose();

function nestedInnerWins() {
  let inner = { x: "in" };
  let outer = { x: "out" };
  let out;
  with (outer) {
    with (inner) {
      out = "\u{41}" + x;
    }
  }
  return out === "Ain";
}
let ai = nestedInnerWins();

function nestedInnerMissOuter() {
  let inner = {};
  let outer = { x: "out" };
  let out;
  with (outer) {
    with (inner) {
      out = "\x41" + x;
    }
  }
  return out === "Aout";
}
let aj = nestedInnerMissOuter();

function ownUndefinedHasBinding() {
  let x = "out";
  let obj = { x: undefined };
  let out;
  with (obj) {
    out = "\u0041" + x;
  }
  return out === "Aundefined" && x === "out";
}
let ak = ownUndefinedHasBinding();

function cookedTemplateEscapes() {
  let env = { s: "x" };
  let esc;
  with (env) {
    esc = `\u0041\x42${s}`;
  }
  return esc === "ABx";
}
let al = cookedTemplateEscapes();

function concatChainUnicode() {
  let obj = { x: "a", y: "b" };
  let out;
  with (obj) {
    out = "\x41" + x + "\u0042" + y + "\u{43}";
  }
  return out === "AaBbC";
}
let am = concatChainUnicode();

function identEscapeUnscopables() {
  let x = "out";
  let obj = { x: "in" };
  obj[Symbol.unscopables] = { x: true };
  let out;
  with (obj) {
    out = "\x41" + \u0078;
  }
  return out === "Aout" && obj.x === "in";
}
let an = identEscapeUnscopables();

function identEscapeLiveClose() {
  let obj = { x: "a" };
  let run;
  with (obj) {
    run = function () {
      return "\u{21}" + \u{78};
    };
  }
  obj.x = "b";
  return run() === "!b";
}
let ap = identEscapeLiveClose();

function identEscapeUnresolvable() {
  with ({}) {
    try {
      \u0078;
      return false;
    } catch (err) {
      return (
        err instanceof ReferenceError &&
        typeof globalThis.x === "undefined"
      );
    }
  }
}
let aq = identEscapeUnresolvable();

function toStringHook() {
  let seen = null;
  let obj = {
    x: {
      toString: function () {
        seen = this;
        return "T";
      }
    }
  };
  let out;
  with (obj) {
    out = "\x41" + x;
  }
  return out === "AT" && seen === obj.x;
}
let ar = toStringHook();

function spaceAndCopy() {
  let obj = { x: "z" };
  let sp;
  let cp;
  with (obj) {
    sp = "\u0020" + x;
    cp = "\u00A9" + x;
  }
  return sp === " z" && cp === "\u00A9z" && cp.length === 2;
}
let at = spaceAndCopy();

function getterRunsOncePerUse() {
  let n = 0;
  let obj = {
    get x() {
      n = n + 1;
      return "v";
    }
  };
  let out;
  with (obj) {
    out = "\x41" + x + "\u0042" + x;
  }
  return out === "AvBv" && n === 2;
}
let au = getterRunsOncePerUse();

function nestedWithUnscopables() {
  let x = "lex";
  let inner = { x: "in" };
  inner[Symbol.unscopables] = { x: true };
  let outer = { x: "out" };
  let out;
  with (outer) {
    with (inner) {
      out = "\u{41}" + x;
    }
  }
  return out === "Aout";
}
let av = nestedWithUnscopables();

function unresolvableNotImplicitGlobal() {
  with ({}) {
    try {
      `${e170276_noglob}`;
      return false;
    } catch (err) {
      return (
        err instanceof ReferenceError &&
        typeof globalThis.e170276_noglob === "undefined"
      );
    }
  }
}
let aw = unresolvableNotImplicitGlobal();

function unresolvableSecondAfterFirst() {
  let ran = false;
  let obj = {
    get x() {
      ran = true;
      return "x";
    }
  };
  with (obj) {
    try {
      "\x41" + x + e170276_second;
      return false;
    } catch (err) {
      return err instanceof ReferenceError && ran === true;
    }
  }
}
let ax = unresolvableSecondAfterFirst();

function nestedTemplateUnresolvable() {
  with ({}) {
    try {
      `outer ${`\u0041 ${e170276_nested}`}`;
      return false;
    } catch (err) {
      return (
        err instanceof ReferenceError &&
        typeof globalThis.e170276_nested === "undefined"
      );
    }
  }
}
let ay = nestedTemplateUnresolvable();

function liveCloseAfterMutateOuter() {
  let x = "lex";
  let obj = {};
  let run;
  with (obj) {
    run = function () {
      return "\x41" + x;
    };
  }
  x = "later";
  return run() === "Alater";
}
let az = liveCloseAfterMutateOuter();

function hexLowConcat() {
  let obj = { x: "Z" };
  let out;
  with (obj) {
    out = "\x61" + x;
  }
  return out === "aZ";
}
let ba = hexLowConcat();

function identEscapeNestedWith() {
  let inner = { x: "in" };
  let outer = { x: "out" };
  let innerOut;
  let outerOut;
  with (outer) {
    with (inner) {
      innerOut = \u0078;
    }
    outerOut = \u{78};
  }
  return innerOut === "in" && outerOut === "out";
}
let bb = identEscapeNestedWith();

function ffCpConcat() {
  let obj = { x: "z" };
  let out;
  with (obj) {
    out = "\u{FF}" + x;
  }
  return out === "\u00FFz" && out.length === 2;
}
let bc = ffCpConcat();

function typeofStringResult() {
  let obj = { x: 1 };
  let t;
  with (obj) {
    t = typeof ("\u0041" + x);
  }
  return t === "string";
}
let bd = typeofStringResult();

function inheritedAccessorThis() {
  let proto = {
    get x() {
      return this === obj ? "in" : "no";
    }
  };
  let obj = Object.create(proto);
  let out;
  with (obj) {
    out = "\x41" + x;
  }
  return out === "Ain";
}
let be = inheritedAccessorThis();

function unscopablesSecondIdent() {
  let y = "out";
  let obj = { x: "in", y: "IN" };
  obj[Symbol.unscopables] = { y: true };
  let out;
  with (obj) {
    out = x + "\u0041" + y;
  }
  return out === "inAout" && obj.y === "IN";
}
let bf = unscopablesSecondIdent();

function templateInterpUnresolvable() {
  with ({}) {
    try {
      `\x41${e170276_tmiss}`;
      return false;
    } catch (err) {
      return (
        err instanceof ReferenceError &&
        typeof globalThis.e170276_tmiss === "undefined"
      );
    }
  }
}
let bg = templateInterpUnresolvable();

function identEscapeAccessorThis() {
  let seen = null;
  let obj = {
    get x() {
      seen = this;
      return "in";
    }
  };
  let out;
  with (obj) {
    out = \u0078;
  }
  return out === "in" && seen === obj;
}
let bh = identEscapeAccessorThis();
