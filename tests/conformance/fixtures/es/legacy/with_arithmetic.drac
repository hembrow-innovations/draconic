// E17.02.60: with + numeric arithmetic through object environment (non-strict).
// LHS/RHS idents hit object (else outer); both sides always evaluated (no
// short-circuit); unary +/- ident hits object; grouping/precedence; inherited
// HasBinding; accessor this is the with object; unresolvable LHS or RHS →
// ReferenceError; live close; nested with; unscopables skip.

function addHitsObject() {
  let obj = { x: 1, y: 2 };
  with (obj) {
    return (x + y) === 3;
  }
}
let a = addHitsObject();

function subHitsObject() {
  let obj = { x: 10, y: 4 };
  with (obj) {
    return (x - y) === 6;
  }
}
let b = subHitsObject();

function mulHitsObject() {
  let obj = { x: 3, y: 4 };
  with (obj) {
    return (x * y) === 12;
  }
}
let c = mulHitsObject();

function divHitsObject() {
  let obj = { x: 20, y: 5 };
  with (obj) {
    return (x / y) === 4;
  }
}
let d = divHitsObject();

function remHitsObject() {
  let obj = { x: 10, y: 3 };
  with (obj) {
    return (x % y) === 1;
  }
}
let e = remHitsObject();

function unaryMinusHitsObject() {
  let obj = { x: 5 };
  with (obj) {
    return -x === -5;
  }
}
let f = unaryMinusHitsObject();

function unaryPlusHitsObject() {
  let obj = { x: "7" };
  with (obj) {
    return +x === 7;
  }
}
let g = unaryPlusHitsObject();

function precedenceMulOverAdd() {
  let obj = { x: 1, y: 2, z: 3 };
  with (obj) {
    return (x + y * z) === 7;
  }
}
let h = precedenceMulOverAdd();

function groupingOverrides() {
  let obj = { x: 1, y: 2, z: 3 };
  with (obj) {
    return ((x + y) * z) === 9;
  }
}
let i = groupingOverrides();

function missIsOuter() {
  let x = 1;
  let y = 2;
  with ({}) {
    return (x + y) === 3;
  }
}
let j = missIsOuter();

function objectShadowsOuter() {
  let x = 9;
  let y = 0;
  let obj = { x: 1, y: 2 };
  with (obj) {
    return (x + y) === 3;
  }
}
let k = objectShadowsOuter();

function bothSidesAlwaysEvaled() {
  let n = 0;
  let obj = {
    get x() {
      n = n + 1;
      return 1;
    },
    get y() {
      n = n + 1;
      return 2;
    }
  };
  with (obj) {
    return (x + y) === 3 && n === 2;
  }
}
let l = bothSidesAlwaysEvaled();

function unscopablesUsesOuter() {
  let x = 1;
  let y = 2;
  let obj = { x: 9, y: 0 };
  obj[Symbol.unscopables] = { x: true, y: true };
  with (obj) {
    return (x + y) === 3;
  }
}
let m = unscopablesUsesOuter();

function inheritedData() {
  let proto = { x: 1, y: 2 };
  let obj = Object.create(proto);
  with (obj) {
    return (x + y) === 3;
  }
}
let p = inheritedData();

function inheritedGetterThis() {
  let seen = null;
  let proto = {
    x: 1,
    get y() {
      seen = this;
      return 2;
    }
  };
  let obj = Object.create(proto);
  with (obj) {
    return (x + y) === 3 && seen === obj;
  }
}
let q = inheritedGetterThis();

function liveClose() {
  let obj = { x: 1, y: 2 };
  let run;
  with (obj) {
    run = function () {
      return x + y;
    };
  }
  obj.y = 10;
  return run() === 11;
}
let r = liveClose();

function unresolvableLhsThrows() {
  with ({}) {
    try {
      e170260_missing + 1;
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let s = unresolvableLhsThrows();

function unresolvableRhsThrowsAlways() {
  let obj = { x: 0 };
  with (obj) {
    try {
      x + e170260_missing_rhs;
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let t = unresolvableRhsThrowsAlways();

function nestedInnerWins() {
  let inner = { x: 1, y: 2 };
  let outer = { x: 9, y: 0 };
  with (outer) {
    with (inner) {
      return (x + y) === 3;
    }
  }
}
let u = nestedInnerWins();

function accessorThisIsWithObject() {
  let seen = null;
  let obj = {
    x: 1,
    get y() {
      seen = this;
      return 2;
    }
  };
  with (obj) {
    return (x + y) === 3 && seen === obj;
  }
}
let v = accessorThisIsWithObject();

function unaryMissIsOuter() {
  let x = 5;
  with ({}) {
    return -x === -5;
  }
}
let w = unaryMissIsOuter();

function chainAddSub() {
  let obj = { x: 1, y: 2, z: 3 };
  with (obj) {
    return (x + y - z) === 0;
  }
}
let aa = chainAddSub();

function unaryUnresolvableThrows() {
  with ({}) {
    try {
      -e170260_missing_unary;
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let ab = unaryUnresolvableThrows();
