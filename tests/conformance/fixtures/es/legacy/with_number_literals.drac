// E17.02.78: with + Number literals (decimal floats, scientific e/E, hex
// 0x, binary 0b, octal 0o, numeric separators _, leading-dot .5) through
// object environment (non-strict). Ident compared/combined with those
// literals hits object (else outer); empty/0; nested; inherited HasBinding;
// accessor get this is with object; unresolvable ident → ReferenceError;
// live close; nested with; unscopables skip.

function decimalHitsObject() {
  let n = 0;
  let obj = { n: 42 };
  let out;
  with (obj) {
    out = n === 42;
  }
  return out === true && n === 0;
}
let a = decimalHitsObject();

function floatHitsObject() {
  let n = 0;
  let obj = { n: 3.14 };
  let out;
  with (obj) {
    out = n === 3.14;
  }
  return out === true && n === 0;
}
let b = floatHitsObject();

function leadingDotHitsObject() {
  let n = 0;
  let obj = { n: 0.5 };
  let out;
  with (obj) {
    out = n === .5;
  }
  return out === true && n === 0;
}
let c = leadingDotHitsObject();

function trailingDotEqFive() {
  let n = 0;
  let obj = { n: 5 };
  let out;
  with (obj) {
    out = n === 5.0;
  }
  return out === true && n === 0;
}
let d = trailingDotEqFive();

function sciLowerHitsObject() {
  let n = 0;
  let obj = { n: 1000 };
  let out;
  with (obj) {
    out = n === 1e3;
  }
  return out === true && n === 0;
}
let e = sciLowerHitsObject();

function sciUpperHitsObject() {
  let n = 0;
  let obj = { n: 1000 };
  let out;
  with (obj) {
    out = n === 1E3;
  }
  return out === true && n === 0;
}
let f = sciUpperHitsObject();

function sciFracHitsObject() {
  let n = 0;
  let obj = { n: 150 };
  let out;
  with (obj) {
    out = n === 1.5e2;
  }
  return out === true && n === 0;
}
let g = sciFracHitsObject();

function sciPlusHitsObject() {
  let n = 0;
  let obj = { n: 200 };
  let out;
  with (obj) {
    out = n === 2e+2;
  }
  return out === true && n === 0;
}
let h = sciPlusHitsObject();

function sciMinusHitsObject() {
  let n = 0;
  let obj = { n: 0.1 };
  let out;
  with (obj) {
    out = n === 1e-1;
  }
  return out === true && n === 0;
}
let i = sciMinusHitsObject();

function hexLowerHitsObject() {
  let n = 0;
  let obj = { n: 255 };
  let out;
  with (obj) {
    out = n === 0xff;
  }
  return out === true && n === 0;
}
let j = hexLowerHitsObject();

function hexUpperHitsObject() {
  let n = 0;
  let obj = { n: 255 };
  let out;
  with (obj) {
    out = n === 0xFF;
  }
  return out === true && n === 0;
}
let k = hexUpperHitsObject();

function binLowerHitsObject() {
  let n = 0;
  let obj = { n: 10 };
  let out;
  with (obj) {
    out = n === 0b1010;
  }
  return out === true && n === 0;
}
let l = binLowerHitsObject();

function binUpperHitsObject() {
  let n = 0;
  let obj = { n: 10 };
  let out;
  with (obj) {
    out = n === 0B1010;
  }
  return out === true && n === 0;
}
let m = binUpperHitsObject();

function octLowerHitsObject() {
  let n = 0;
  let obj = { n: 15 };
  let out;
  with (obj) {
    out = n === 0o17;
  }
  return out === true && n === 0;
}
let n = octLowerHitsObject();

function octUpperHitsObject() {
  let n = 0;
  let obj = { n: 15 };
  let out;
  with (obj) {
    out = n === 0O17;
  }
  return out === true && n === 0;
}
let p = octUpperHitsObject();

function sepDecimalHitsObject() {
  let n = 0;
  let obj = { n: 1000 };
  let out;
  with (obj) {
    out = n === 1_000;
  }
  return out === true && n === 0;
}
let q = sepDecimalHitsObject();

function sepHexHitsObject() {
  let n = 0;
  let obj = { n: 65535 };
  let out;
  with (obj) {
    out = n === 0xFF_FF;
  }
  return out === true && n === 0;
}
let r = sepHexHitsObject();

function sepBinHitsObject() {
  let n = 0;
  let obj = { n: 161 };
  let out;
  with (obj) {
    out = n === 0b1010_0001;
  }
  return out === true && n === 0;
}
let s = sepBinHitsObject();

function sepFloatHitsObject() {
  let n = 0;
  let obj = { n: 1000.5 };
  let out;
  with (obj) {
    out = n === 1_000.5_00;
  }
  return out === true && n === 0;
}
let t = sepFloatHitsObject();

function sepSciHitsObject() {
  let n = 0;
  let obj = { n: 10000000000 };
  let out;
  with (obj) {
    out = n === 1e1_0;
  }
  return out === true && n === 0;
}
let u = sepSciHitsObject();

function mixFormsHitsObject() {
  let n = 0;
  let obj = { n: 10 };
  let out;
  with (obj) {
    out = 0x10 + 0b10 + 0o10 + n;
  }
  return out === 36 && n === 0;
}
let v = mixFormsHitsObject();

function missUsesOuter() {
  let n = 42;
  let obj = {};
  let out;
  with (obj) {
    out = n === 0x2a;
  }
  return out === true;
}
let w = missUsesOuter();

function mixedObjectOuter() {
  let m = 0b10;
  let obj = { n: 0xff };
  let out;
  with (obj) {
    out = n + m;
  }
  return out === 257 && m === 2;
}
let aa = mixedObjectOuter();

function zeroHitsObject() {
  let n = 1;
  let obj = { n: 0 };
  let out;
  with (obj) {
    out = n === 0;
  }
  return out === true && n === 1;
}
let ab = zeroHitsObject();

function typeofNumber() {
  let obj = { n: 0x10 };
  let t;
  with (obj) {
    t = typeof n;
  }
  return t === "number";
}
let ac = typeofNumber();

function eqAcrossForms() {
  let obj = { n: 255 };
  let hex;
  let bin;
  let oct;
  with (obj) {
    hex = n === 0xff;
    bin = n === 0b11111111;
    oct = n === 0o377;
  }
  return hex === true && bin === true && oct === true;
}
let ad = eqAcrossForms();

function hexIndexHitsObject() {
  let k = {};
  let obj = { k: { 255: "hit" } };
  let out;
  with (obj) {
    out = k[0xff];
  }
  return out === "hit" && k[0xff] === undefined;
}
let ae = hexIndexHitsObject();

function binIndexHitsObject() {
  let k = {};
  let obj = { k: { 10: "ten" } };
  let out;
  with (obj) {
    out = k[0b1010];
  }
  return out === "ten";
}
let af = binIndexHitsObject();

function sciAddIdent() {
  let n = 0;
  let obj = { n: 50 };
  let out;
  with (obj) {
    out = n + 1.5e2;
  }
  return out === 200 && n === 0;
}
let ag = sciAddIdent();

function leadingDotMissUsesOuter() {
  let n = .5;
  let obj = {};
  let out;
  with (obj) {
    out = n === 0.5;
  }
  return out === true;
}
let ah = leadingDotMissUsesOuter();

function unscopablesSkip() {
  let n = 1;
  let obj = { n: 99 };
  obj[Symbol.unscopables] = { n: true };
  let out;
  with (obj) {
    out = n === 0x1;
  }
  return out === true && obj.n === 99;
}
let ai = unscopablesSkip();

function inheritedHasBinding() {
  let proto = { n: 0xff };
  let obj = Object.create(proto);
  let out;
  with (obj) {
    out = n === 255;
  }
  return out === true;
}
let aj = inheritedHasBinding();

function accessorGetThis() {
  let seen = null;
  let obj = {
    get n() {
      seen = this;
      return 0o17;
    }
  };
  let out;
  with (obj) {
    out = n === 15;
  }
  return out === true && seen === obj;
}
let ak = accessorGetThis();

function unresolvableThrows() {
  with ({}) {
    try {
      e170278_missing === 0xff;
      return false;
    } catch (err) {
      return (
        err instanceof ReferenceError &&
        typeof globalThis.e170278_missing === "undefined"
      );
    }
  }
}
let al = unresolvableThrows();

function unresolvableSkipsLater() {
  let ran = false;
  let obj = {
    get n() {
      ran = true;
      return 1;
    }
  };
  with (obj) {
    try {
      e170278_first + n;
      return false;
    } catch (err) {
      return err instanceof ReferenceError && ran === false;
    }
  }
}
let am = unresolvableSkipsLater();

function liveClose() {
  let obj = { n: 1 };
  let run;
  with (obj) {
    run = function () {
      return n === 0xff;
    };
  }
  obj.n = 255;
  return run() === true;
}
let an = liveClose();

function nestedInnerWins() {
  let inner = { n: 0b1010 };
  let outer = { n: 0xff };
  let out;
  with (outer) {
    with (inner) {
      out = n === 10;
    }
  }
  return out === true;
}
let ap = nestedInnerWins();

function nestedInnerMissOuter() {
  let inner = {};
  let outer = { n: 0x10 };
  let out;
  with (outer) {
    with (inner) {
      out = n === 16;
    }
  }
  return out === true;
}
let aq = nestedInnerMissOuter();

function ownUndefinedHasBinding() {
  let n = 42;
  let obj = { n: undefined };
  let out;
  with (obj) {
    out = n === undefined;
  }
  return out === true && n === 42;
}
let ar = ownUndefinedHasBinding();

function getterRunsOncePerUse() {
  let c = 0;
  let obj = {
    get n() {
      c = c + 1;
      return 0x10;
    }
  };
  let out;
  with (obj) {
    out = n + n;
  }
  return out === 32 && c === 2;
}
let at = getterRunsOncePerUse();

function nestedWithUnscopables() {
  let n = 1;
  let inner = { n: 99 };
  inner[Symbol.unscopables] = { n: true };
  let outer = { n: 0xfe };
  let out;
  with (outer) {
    with (inner) {
      out = n === 254;
    }
  }
  return out === true;
}
let au = nestedWithUnscopables();

function unresolvableNotImplicitGlobal() {
  with ({}) {
    try {
      e170278_noglob + 0x1;
      return false;
    } catch (err) {
      return (
        err instanceof ReferenceError &&
        typeof globalThis.e170278_noglob === "undefined"
      );
    }
  }
}
let av = unresolvableNotImplicitGlobal();

function unresolvableSecondAfterFirst() {
  let ran = false;
  let obj = {
    get n() {
      ran = true;
      return 1;
    }
  };
  with (obj) {
    try {
      n + e170278_second;
      return false;
    } catch (err) {
      return err instanceof ReferenceError && ran === true;
    }
  }
}
let aw = unresolvableSecondAfterFirst();

function liveCloseAfterMutateOuter() {
  let n = 1;
  let obj = { n: 2 };
  let run;
  with (obj) {
    run = function () {
      return n === 0b1010;
    };
  }
  obj.n = 10;
  n = 99;
  return run() === true && n === 99;
}
let ax = liveCloseAfterMutateOuter();

function valueOfHook() {
  let seen = null;
  let obj = {
    n: {
      valueOf: function () {
        seen = this;
        return 0xff;
      }
    }
  };
  let out;
  with (obj) {
    out = n + 0;
  }
  return out === 255 && seen === obj.n;
}
let ay = valueOfHook();

function inheritedAccessorThis() {
  let proto = {
    get n() {
      return this === obj ? 0o17 : 0;
    }
  };
  let obj = Object.create(proto);
  let out;
  with (obj) {
    out = n === 15;
  }
  return out === true;
}
let az = inheritedAccessorThis();

function unscopablesSecondIdent() {
  let m = 1;
  let obj = { n: 0x10, m: 99 };
  obj[Symbol.unscopables] = { m: true };
  let out;
  with (obj) {
    out = n + m;
  }
  return out === 17 && obj.m === 99;
}
let ba = unscopablesSecondIdent();

function nestedLiteralAdd() {
  let obj = { n: 1, m: 2 };
  let out;
  with (obj) {
    out = n + 0x10 + m + 0b10;
  }
  return out === 21;
}
let bb = nestedLiteralAdd();

function unaryMinusVsLiteral() {
  let n = 0;
  let obj = { n: 5 };
  let out;
  with (obj) {
    out = -n === -0x5;
  }
  return out === true && n === 0;
}
let bc = unaryMinusVsLiteral();

function groupingLiteral() {
  let obj = { n: 2 };
  let out;
  with (obj) {
    out = (0x10 + n) * 0b10;
  }
  return out === 36;
}
let bd = groupingLiteral();

function octSepHitsObject() {
  let n = 0;
  let obj = { n: 15 };
  let out;
  with (obj) {
    out = n === 0o1_7;
  }
  return out === true && n === 0;
}
let be = octSepHitsObject();

function hexAddOuter() {
  let m = 0x10;
  let obj = { n: 0b10 };
  let out;
  with (obj) {
    out = n + m;
  }
  return out === 18 && m === 16;
}
let bf = hexAddOuter();

function typeofMixResult() {
  let obj = { n: 1 };
  let t;
  with (obj) {
    t = typeof (n + 0xff);
  }
  return t === "number";
}
let bg = typeofMixResult();
