// E17.02.125: with + labelled function declarations (Annex B.3.2:
// `label: function f(){…}`, non-strict) through object environment.
// Body idents hit object (else outer); labels are not bindings
// (same-name object prop still a label); name hoisted enclosing VE
// (not the object); assigned when labelled statement runs; function
// closes over object env (live); nested labels; let in function does
// not write object; inherited HasBinding; accessor this is with object;
// unresolvable → ReferenceError; live close; nested with;
// unscopables skip.

function bodyIdentHitsObject() {
  let obj = { x: 5 };
  with (obj) {
    L: function f() {
      return x;
    }
  }
  return f() === 5;
}
let a = bodyIdentHitsObject();

function bodyIdentMissUsesOuter() {
  let x = 5;
  with ({}) {
    L: function f() {
      return x;
    }
  }
  return f() === 5;
}
let b = bodyIdentMissUsesOuter();

function bodyIdentShadowsOuter() {
  let x = 0;
  let obj = { x: 5 };
  with (obj) {
    L: function f() {
      return x;
    }
  }
  return f() === 5 && x === 0;
}
let c = bodyIdentShadowsOuter();

function nameHoistedEnclosingNotObject() {
  let obj = {};
  with (obj) {
    L: function f() {
      return 1;
    }
  }
  return (
    typeof f === "function" &&
    f() === 1 &&
    Object.prototype.hasOwnProperty.call(obj, "f") === false &&
    obj.f === undefined
  );
}
let d = nameHoistedEnclosingNotObject();

function typeofBeforeWith() {
  let before = typeof f;
  with ({}) {
    L: function f() {
      return 7;
    }
  }
  return before === "undefined" && typeof f === "function" && f() === 7;
}
let e = typeofBeforeWith();

function nestedLabels() {
  let obj = { x: 2 };
  with (obj) {
    A: B: function g() {
      return x;
    }
  }
  return typeof g === "function" && g() === 2;
}
let f = nestedLabels();

function labelIsNotBinding() {
  let obj = { x: 1 };
  with (obj) {
    x: function h() {
      return 3;
    }
  }
  return h() === 3 && obj.x === 1;
}
let g = labelIsNotBinding();

function liveClose() {
  let obj = { x: 1 };
  with (obj) {
    L: function f() {
      return x;
    }
  }
  obj.x = 9;
  return f() === 9;
}
let h = liveClose();

function callInsideWith() {
  let obj = { x: 4 };
  let out;
  with (obj) {
    L: function f() {
      return x;
    }
    out = f();
  }
  return out === 4;
}
let i = callInsideWith();

function skippedBranchNotAssigned() {
  if (false) {
    with ({}) {
      L: function k() {
        return 1;
      }
    }
  }
  return typeof k === "undefined";
}
let j = skippedBranchNotAssigned();

function paramsShadowObject() {
  let obj = { x: 1 };
  with (obj) {
    L: function f(x) {
      return x;
    }
  }
  return f(4) === 4 && f() === undefined;
}
let k = paramsShadowObject();

function letInFunctionDoesNotWriteObject() {
  let obj = { x: 1 };
  with (obj) {
    L: function f() {
      let x = 5;
      x = 6;
      return x;
    }
  }
  return f() === 6 && obj.x === 1;
}
let l = letInFunctionDoesNotWriteObject();

function unresolvableThrows() {
  with ({}) {
    L: function f() {
      return e1702125_missing;
    }
  }
  try {
    f();
    return false;
  } catch (err) {
    return (
      err instanceof ReferenceError &&
      typeof globalThis.e1702125_missing === "undefined"
    );
  }
}
let m = unresolvableThrows();

function accessorThisIsWithObject() {
  let seen = null;
  let obj = {
    get x() {
      seen = this;
      return 1;
    }
  };
  with (obj) {
    L: function f() {
      return x;
    }
  }
  return f() === 1 && seen === obj;
}
let p = accessorThisIsWithObject();

function unscopablesUsesOuter() {
  let x = 1;
  let obj = { x: 99 };
  obj[Symbol.unscopables] = { x: true };
  with (obj) {
    L: function f() {
      return x;
    }
  }
  return f() === 1 && obj.x === 99;
}
let q = unscopablesUsesOuter();

function inheritedHasBinding() {
  let proto = { x: 8 };
  let obj = Object.create(proto);
  with (obj) {
    L: function f() {
      return x;
    }
  }
  return f() === 8;
}
let r = inheritedHasBinding();

function nestedInnerWins() {
  let inner = { x: 1 };
  let outer = { x: 0 };
  with (outer) {
    with (inner) {
      L: function f() {
        return x;
      }
    }
  }
  return f() === 1;
}
let s = nestedInnerWins();

function nestedInnerMissUsesOuter() {
  let inner = {};
  let outer = { x: 3 };
  with (outer) {
    with (inner) {
      L: function f() {
        return x;
      }
    }
  }
  return f() === 3;
}
let t = nestedInnerMissUsesOuter();

function bareCallThisIsGlobal() {
  let obj = { x: 1 };
  with (obj) {
    L: function f() {
      return this === globalThis;
    }
  }
  return f() === true;
}
let u = bareCallThisIsGlobal();

function multipleLabelledFunctions() {
  let obj = { x: 1, y: 2 };
  with (obj) {
    A: function fa() {
      return x;
    }
    B: function fb() {
      return y;
    }
  }
  return fa() === 1 && fb() === 2;
}
let v = multipleLabelledFunctions();

function strictLabelledFunctionSyntaxError() {
  try {
    eval("'use strict'; L: function f() { return 1; }");
    return false;
  } catch (err) {
    return err instanceof SyntaxError;
  }
}
let w = strictLabelledFunctionSyntaxError();

function letNotOverwrittenByLabelledInWith() {
  let y = 1;
  with ({}) {
    L: function y() {
      return 2;
    }
  }
  return y === 1;
}
let y = letNotOverwrittenByLabelledInWith();

function constNotOverwrittenByLabelledInWith() {
  const y = 1;
  with ({}) {
    L: function y() {
      return 2;
    }
  }
  return y === 1;
}
let aa = constNotOverwrittenByLabelledInWith();

function nestedWithLabelledFn() {
  let inner = { x: 2 };
  let outer = { x: 1 };
  with (outer) {
    with (inner) {
      L: function n() {
        return x;
      }
    }
  }
  inner.x = 8;
  return n() === 8;
}
let ab = nestedWithLabelledFn();

function liveCloseAfterMutateOuter() {
  let x = 1;
  let obj = { x: 2 };
  with (obj) {
    L: function f() {
      return x;
    }
  }
  obj.x = 10;
  x = 99;
  return f() === 10 && x === 99;
}
let ac = liveCloseAfterMutateOuter();

function ownUndefinedHasBinding() {
  let x = 42;
  let obj = { x: undefined };
  with (obj) {
    L: function f() {
      return x;
    }
  }
  return f() === undefined && x === 42;
}
let ad = ownUndefinedHasBinding();

function getterRunsOncePerUse() {
  let c = 0;
  let obj = {
    get x() {
      c = c + 1;
      return 3;
    }
  };
  with (obj) {
    L: function f() {
      return x + x;
    }
  }
  return f() === 6 && c === 2;
}
let ae = getterRunsOncePerUse();

function nestedWithUnscopables() {
  let x = 1;
  let inner = { x: 99 };
  inner[Symbol.unscopables] = { x: true };
  let outer = { x: 7 };
  with (outer) {
    with (inner) {
      L: function f() {
        return x;
      }
    }
  }
  return f() === 7;
}
let af = nestedWithUnscopables();

function unresolvableNotImplicitGlobal() {
  with ({}) {
    L: function f() {
      return e1702125_noglob;
    }
  }
  try {
    f();
    return false;
  } catch (err) {
    return (
      err instanceof ReferenceError &&
      typeof globalThis.e1702125_noglob === "undefined"
    );
  }
}
let ag = unresolvableNotImplicitGlobal();

function inheritedAccessorThis() {
  let proto = {
    get x() {
      return this === obj ? 8 : 0;
    }
  };
  let obj = Object.create(proto);
  with (obj) {
    L: function f() {
      return x;
    }
  }
  return f() === 8;
}
let ah = inheritedAccessorThis();

function unscopablesFalseyStillObject() {
  let x = 0;
  let obj = { x: 1 };
  obj[Symbol.unscopables] = { x: false };
  with (obj) {
    L: function f() {
      return x;
    }
  }
  return f() === 1 && obj.x === 1;
}
let aj = unscopablesFalseyStillObject();

function labelledFnInNestedBlock() {
  let obj = { x: 4 };
  with (obj) {
    {
      L: function f() {
        return x;
      }
    }
  }
  return f() === 4;
}
let ak = labelledFnInNestedBlock();

function functionNameSameAsObjectProp() {
  let obj = { f: 99, x: 2 };
  with (obj) {
    L: function f() {
      return x;
    }
  }
  return f() === 2 && obj.f === 99;
}
let al = functionNameSameAsObjectProp();

function defaultParamHitsObject() {
  let obj = { x: 7 };
  with (obj) {
    L: function f(p) {
      if (p === undefined) {
        p = x;
      }
      return p;
    }
  }
  return f() === 7 && f(1) === 1;
}
let am = defaultParamHitsObject();

function assignedWhenStatementRuns() {
  if (true) {
    with ({}) {
      L: function f() {
        return 5;
      }
    }
  }
  return typeof f === "function" && f() === 5;
}
let an = assignedWhenStatementRuns();

function missAssignFromBodyFallsThrough() {
  let z = 0;
  with ({}) {
    L: function f() {
      z = 8;
    }
  }
  f();
  return z === 8;
}
let ap = missAssignFromBodyFallsThrough();

function assignHitsObjectFromBody() {
  let obj = { z: 0 };
  with (obj) {
    L: function f() {
      z = 9;
    }
  }
  f();
  return obj.z === 9;
}
let aq = assignHitsObjectFromBody();

function labelSameAsFunctionName() {
  let obj = { x: 1 };
  with (obj) {
    f: function f() {
      return x;
    }
  }
  return f() === 1;
}
let ar = labelSameAsFunctionName();

function strictBodyThisUndefined() {
  let obj = { x: 1 };
  with (obj) {
    L: function f() {
      "use strict";
      return this === undefined;
    }
  }
  return f() === true;
}
let at = strictBodyThisUndefined();

function nestedFnHasOwnArguments() {
  let obj = { arguments: 99 };
  with (obj) {
    L: function f() {
      function inner() {
        return arguments[0];
      }
      return inner(3);
    }
  }
  return f() === 3;
}
let au = nestedFnHasOwnArguments();
