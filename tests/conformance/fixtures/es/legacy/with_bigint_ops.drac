// E17.02.80: with + BigInt comparison & bitwise through object environment
// (non-strict). LHS/RHS idents hit object (else outer); both sides always
// evaluated (no short-circuit); ==/!= coerce, ===/!== do not; unary ~ ident
// hits object; mixed Number TypeError on bitwise; no >>>; inherited
// HasBinding; accessor this is with object; unresolvable LHS or RHS →
// ReferenceError; live close; nested with; unscopables skip.

function ltHitsObject() {
  let obj = { x: 1n, y: 2n };
  with (obj) {
    return (x < y) === true;
  }
}
let a = ltHitsObject();

function leHitsObject() {
  let obj = { x: 2n, y: 2n };
  with (obj) {
    return (x <= y) === true;
  }
}
let b = leHitsObject();

function gtHitsObject() {
  let obj = { x: 3n, y: 1n };
  with (obj) {
    return (x > y) === true;
  }
}
let c = gtHitsObject();

function geHitsObject() {
  let obj = { x: 3n, y: 3n };
  with (obj) {
    return (x >= y) === true;
  }
}
let d = geHitsObject();

function eqNeHitsObject() {
  let obj = { x: 1n, y: 2n };
  with (obj) {
    return (x != y) === true && (x == y) === false;
  }
}
let e = eqNeHitsObject();

function seqSneqHitsObject() {
  let obj = { x: 1n, y: 1 };
  with (obj) {
    return (x == y) === true && (x === y) === false && (x !== y) === true;
  }
}
let f = seqSneqHitsObject();

function andHitsObject() {
  let obj = { x: 0b1100n, y: 0b1010n };
  with (obj) {
    return (x & y) === 0b1000n;
  }
}
let g = andHitsObject();

function orHitsObject() {
  let obj = { x: 0b1100n, y: 0b1010n };
  with (obj) {
    return (x | y) === 0b1110n;
  }
}
let h = orHitsObject();

function xorHitsObject() {
  let obj = { x: 0b1100n, y: 0b1010n };
  with (obj) {
    return (x ^ y) === 0b0110n;
  }
}
let i = xorHitsObject();

function notHitsObject() {
  let obj = { x: 0n };
  with (obj) {
    return ~x === -1n;
  }
}
let j = notHitsObject();

function shlHitsObject() {
  let obj = { x: 1n, y: 3n };
  with (obj) {
    return (x << y) === 8n;
  }
}
let k = shlHitsObject();

function shrHitsObject() {
  let obj = { x: 16n, y: 2n };
  with (obj) {
    return (x >> y) === 4n;
  }
}
let l = shrHitsObject();

function missIsOuter() {
  let x = 1n;
  let y = 2n;
  with ({}) {
    return (x < y) === true && (x & 0b11n) === 1n;
  }
}
let m = missIsOuter();

function objectShadowsOuter() {
  let x = 9n;
  let y = 0n;
  let obj = { x: 1n, y: 2n };
  with (obj) {
    return (x < y) === true;
  }
}
let p = objectShadowsOuter();

function bothSidesAlwaysEvaledCompare() {
  let n = 0;
  let obj = {
    get x() {
      n = n + 1;
      return 0n;
    },
    get y() {
      n = n + 1;
      return 1n;
    }
  };
  with (obj) {
    return (x < y) === true && n === 2;
  }
}
let q = bothSidesAlwaysEvaledCompare();

function bothSidesAlwaysEvaledBitwise() {
  let n = 0;
  let obj = {
    get x() {
      n = n + 1;
      return 0b1100n;
    },
    get y() {
      n = n + 1;
      return 0b1010n;
    }
  };
  with (obj) {
    return (x & y) === 0b1000n && n === 2;
  }
}
let r = bothSidesAlwaysEvaledBitwise();

function unscopablesUsesOuter() {
  let x = 1n;
  let y = 2n;
  let obj = { x: 9n, y: 0n };
  obj[Symbol.unscopables] = { x: true, y: true };
  with (obj) {
    return (x < y) === true;
  }
}
let s = unscopablesUsesOuter();

function inheritedData() {
  let proto = { x: 1n, y: 2n };
  let obj = Object.create(proto);
  with (obj) {
    return (x < y) === true && (x | y) === 3n;
  }
}
let t = inheritedData();

function inheritedGetterThis() {
  let seen = null;
  let proto = {
    x: 1n,
    get y() {
      seen = this;
      return 2n;
    }
  };
  let obj = Object.create(proto);
  with (obj) {
    return (x < y) === true && seen === obj;
  }
}
let u = inheritedGetterThis();

function liveClose() {
  let obj = { x: 1n, y: 2n };
  let run;
  with (obj) {
    run = function () {
      return x < y;
    };
  }
  obj.y = 0n;
  return run() === false;
}
let v = liveClose();

function liveCloseBitwise() {
  let obj = { x: 0b1100n, y: 0b1010n };
  let run;
  with (obj) {
    run = function () {
      return x & y;
    };
  }
  obj.y = 0b0001n;
  return run() === 0n;
}
let w = liveCloseBitwise();

function unresolvableLhsThrows() {
  with ({}) {
    try {
      e170280_missing < 1n;
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let aa = unresolvableLhsThrows();

function unresolvableRhsThrowsAlways() {
  let obj = { x: 0n };
  with (obj) {
    try {
      x < e170280_missing_rhs;
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let ab = unresolvableRhsThrowsAlways();

function unresolvableBitwiseLhs() {
  with ({}) {
    try {
      e170280_bit_lhs & 1n;
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let ac = unresolvableBitwiseLhs();

function unresolvableBitwiseRhs() {
  let obj = { x: 0n };
  with (obj) {
    try {
      x & e170280_bit_rhs;
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let ad = unresolvableBitwiseRhs();

function nestedInnerWins() {
  let inner = { x: 1n, y: 2n };
  let outer = { x: 9n, y: 0n };
  with (outer) {
    with (inner) {
      return (x < y) === true;
    }
  }
}
let ae = nestedInnerWins();

function nestedInnerMissOuter() {
  let inner = {};
  let outer = { x: 0xffn, y: 0b10n };
  with (outer) {
    with (inner) {
      return (x > y) === true && (x & y) === 2n;
    }
  }
}
let af = nestedInnerMissOuter();

function accessorThisIsWithObject() {
  let seen = null;
  let obj = {
    x: 1n,
    get y() {
      seen = this;
      return 2n;
    }
  };
  with (obj) {
    return (x < y) === true && seen === obj;
  }
}
let ag = accessorThisIsWithObject();

function unaryMissIsOuter() {
  let x = 0n;
  with ({}) {
    return ~x === -1n;
  }
}
let ah = unaryMissIsOuter();

function unaryUnresolvableThrows() {
  with ({}) {
    try {
      ~e170280_missing_unary;
      return false;
    } catch (err) {
      return err instanceof ReferenceError;
    }
  }
}
let ai = unaryUnresolvableThrows();

function mixedRelational() {
  let obj = { x: 1n, y: 2 };
  with (obj) {
    return (x < y) === true && (x <= y) === true && (x > y) === false;
  }
}
let aj = mixedRelational();

function mixedAbstractEq() {
  let obj = { x: 1n, y: 1 };
  with (obj) {
    return (x == y) === true && (x != y) === false;
  }
}
let ak = mixedAbstractEq();

function mixedStrictNeq() {
  let obj = { x: 1n, y: 1 };
  with (obj) {
    return (x === y) === false && (x !== y) === true;
  }
}
let al = mixedStrictNeq();

function mixedBitwiseTypeError() {
  let obj = { x: 1n, y: 1 };
  with (obj) {
    try {
      x & y;
      return false;
    } catch (err) {
      return err instanceof TypeError;
    }
  }
}
let am = mixedBitwiseTypeError();

function mixedBitwiseNumberLeft() {
  let obj = { x: 1, y: 1n };
  with (obj) {
    try {
      x | y;
      return false;
    } catch (err) {
      return err instanceof TypeError;
    }
  }
}
let an = mixedBitwiseNumberLeft();

function ushrTypeError() {
  let obj = { x: 1n, y: 1n };
  with (obj) {
    try {
      x >>> y;
      return false;
    } catch (err) {
      return err instanceof TypeError;
    }
  }
}
let ap = ushrTypeError();

function leFalseWhenGreater() {
  let obj = { x: 3n, y: 1n };
  with (obj) {
    return (x <= y) === false && (x >= y) === true;
  }
}
let aq = leFalseWhenGreater();

function negativeCompare() {
  let obj = { x: -1n, y: 0n };
  with (obj) {
    return (x < y) === true && (x > y) === false;
  }
}
let ar = negativeCompare();

function hexBinCompare() {
  let obj = { x: 0xffn, y: 0b10n };
  with (obj) {
    return (x > y) === true && (x === 255n) === true;
  }
}
let at = hexBinCompare();

function negativeShr() {
  let obj = { x: -8n, y: 2n };
  with (obj) {
    return (x >> y) === -2n;
  }
}
let au = negativeShr();

function notNegOne() {
  let obj = { x: -1n };
  with (obj) {
    return ~x === 0n;
  }
}
let av = notNegOne();

function unscopablesSecondIdent() {
  let y = 2n;
  let obj = { x: 1n, y: 99n };
  obj[Symbol.unscopables] = { y: true };
  let out;
  with (obj) {
    out = x < y;
  }
  return out === true && obj.y === 99n;
}
let aw = unscopablesSecondIdent();

function nestedWithUnscopables() {
  let x = 1n;
  let y = 2n;
  let inner = { x: 9n, y: 0n };
  inner[Symbol.unscopables] = { x: true, y: true };
  let outer = { x: 3n, y: 4n };
  with (outer) {
    with (inner) {
      return (x < y) === true;
    }
  }
}
let ax = nestedWithUnscopables();

function unresolvableNotImplicitGlobal() {
  with ({}) {
    try {
      e170280_noglob < 1n;
      return false;
    } catch (err) {
      return (
        err instanceof ReferenceError &&
        typeof globalThis.e170280_noglob === "undefined"
      );
    }
  }
}
let ay = unresolvableNotImplicitGlobal();

function unresolvableSecondAfterFirst() {
  let ran = false;
  let obj = {
    get x() {
      ran = true;
      return 1n;
    }
  };
  with (obj) {
    try {
      x < e170280_second;
      return false;
    } catch (err) {
      return err instanceof ReferenceError && ran === true;
    }
  }
}
let az = unresolvableSecondAfterFirst();

function unresolvableFirstSkipsRhs() {
  let ran = false;
  let obj = {
    get y() {
      ran = true;
      return 1n;
    }
  };
  with (obj) {
    try {
      e170280_first & y;
      return false;
    } catch (err) {
      return err instanceof ReferenceError && ran === false;
    }
  }
}
let ba = unresolvableFirstSkipsRhs();

function liveCloseAfterMutateOuter() {
  let x = 1n;
  let obj = { x: 2n, y: 10n };
  let run;
  with (obj) {
    run = function () {
      return x < y;
    };
  }
  obj.x = 11n;
  x = 0n;
  return run() === false && x === 0n;
}
let bb = liveCloseAfterMutateOuter();

function valueOfHookCompare() {
  let seen = null;
  let obj = {
    x: {
      valueOf: function () {
        seen = this;
        return 1n;
      }
    },
    y: 2n
  };
  let out;
  with (obj) {
    out = x < y;
  }
  return out === true && seen === obj.x;
}
let bc = valueOfHookCompare();

function inheritedAccessorThisBitwise() {
  let proto = {
    x: 0b1100n,
    get y() {
      return this === obj ? 0b1010n : 0n;
    }
  };
  let obj = Object.create(proto);
  let out;
  with (obj) {
    out = x & y;
  }
  return out === 0b1000n;
}
let bd = inheritedAccessorThisBitwise();

function chainAndOr() {
  let obj = { x: 15n, y: 7n, z: 8n };
  with (obj) {
    return (x & y | z) === 15n;
  }
}
let be = chainAndOr();

function groupingOverrides() {
  let obj = { x: 1n, y: 2n, z: 4n };
  with (obj) {
    return ((x | y) & z) === 0n;
  }
}
let bf = groupingOverrides();

function mixFormsEq() {
  let obj = { x: 255n };
  with (obj) {
    return x === 0xffn && x === 0b11111111n && x === 0o377n;
  }
}
let bg = mixFormsEq();

function emptyZeroMiss() {
  let x = 0n;
  let obj = {};
  let out;
  with (obj) {
    out = x === 0n && ~x === -1n;
  }
  return out === true;
}
let bh = emptyZeroMiss();

function ownUndefinedHasBinding() {
  let x = 42n;
  let obj = { x: undefined };
  let out;
  with (obj) {
    out = x === undefined;
  }
  return out === true && x === 42n;
}
let bi = ownUndefinedHasBinding();

function getterRunsOncePerUse() {
  let c = 0;
  let obj = {
    get x() {
      c = c + 1;
      return 0x10n;
    }
  };
  let out;
  with (obj) {
    out = x === x;
  }
  return out === true && c === 2;
}
let bj = getterRunsOncePerUse();

function mixedXorTypeError() {
  let obj = { x: 1n };
  with (obj) {
    try {
      x ^ 1;
      return false;
    } catch (err) {
      return err instanceof TypeError;
    }
  }
}
let bk = mixedXorTypeError();

function mixedShlTypeError() {
  let obj = { x: 1n };
  with (obj) {
    try {
      x << 1;
      return false;
    } catch (err) {
      return err instanceof TypeError;
    }
  }
}
let bl = mixedShlTypeError();

function typeofBitwiseResult() {
  let obj = { x: 0xffn, y: 0x0fn };
  let t;
  with (obj) {
    t = typeof (x & y);
  }
  return t === "bigint";
}
let bm = typeofBitwiseResult();

function typeofCompareResult() {
  let obj = { x: 1n, y: 2n };
  let t;
  with (obj) {
    t = typeof (x < y);
  }
  return t === "boolean";
}
let bn = typeofCompareResult();

function sepLiteralCompare() {
  let obj = { x: 1_000n };
  with (obj) {
    return (x >= 1_000n) === true && (x < 1_001n) === true;
  }
}
let bp = sepLiteralCompare();
