// R02.03: CLI flags `--allow-net-listen --allow-net-connect` grant the net subset.
let s = tcpListen(0);
let p = tcpLocalPort(s);
let c = tcpConnect("127.0.0.1", p);
let t = typeof c;
let ok = c > 0;
closeTcp(c);
closeTcp(s);
