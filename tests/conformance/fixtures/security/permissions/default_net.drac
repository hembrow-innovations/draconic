// R02.04: default permission policy is permissive — TCP listen/connect with no explicit grant subset.
let s = tcpListen(0);
let p = tcpLocalPort(s);
let c = tcpConnect("127.0.0.1", p);
let t = typeof c;
let ok = c > 0;
closeTcp(c);
closeTcp(s);
