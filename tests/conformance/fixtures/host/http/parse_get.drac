// H10.01: httpParseRequest GET — method/path/version; empty body; Host header.
let req = httpParseRequest("GET /hello HTTP/1.1\r\nHost: example.com\r\n\r\n");
let m = req.method;
let p = req.path;
let v = req.version;
let b = req.body;
let h = httpRequestHeader(req, "Host");
