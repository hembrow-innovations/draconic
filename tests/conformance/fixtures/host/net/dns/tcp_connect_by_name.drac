// H09.02: tcpConnect hostname (not dotted IPv4) → resolve + dial; typeof number; close.
let s = tcpListen(0);
let p = tcpLocalPort(s);
let c = tcpConnect("localhost", p);
let t = typeof c;
let ok = c > 0;
closeTcp(c);
closeTcp(s);
