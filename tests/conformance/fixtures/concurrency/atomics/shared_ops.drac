let t = typeof makeSharedMemory;
let u = typeof sharedLoad;
let w = typeof sharedStore;
let mem = makeSharedMemory(2);
let live = mem > 0;
let st = sharedStore(mem, 0, 7);
let v = sharedLoad(mem, 0);
let loaded = v === 7;
let old = sharedAdd(mem, 0, 3);
let afterAdd = sharedLoad(mem, 0);
let addedOld = old === 7;
let addedNew = afterAdd === 10;
let cas1 = sharedCompareExchange(mem, 0, 10, 42);
let cas2 = sharedCompareExchange(mem, 0, 10, 99);
let afterCas = sharedLoad(mem, 0);
let casFirst = cas1 === 10;
let casSecond = cas2 === 42;
let casStored = afterCas === 42;
let bad = sharedStore(0, 0, 1);
let invalid = bad < 0;
